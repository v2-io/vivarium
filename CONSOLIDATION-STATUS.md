# Consolidation status — big-picture intuition

*Body updated 2026-07-28 (late): the lede had lagged its own open list — caught by an outside reader, which is exactly the numbers-decay class `#norm-caught-disciplines-become-mechanisms` FE(2)(b) names. **Not claim canon.** When this file disagrees with `core/`, core wins.*

## The picture, in one paragraph

The claim channel is **102 segments** (counted 2026-07-29; the candidates-inventory discharge added lawful-steering, instrument-before-tuning, the staged-hybrid rendering decision, and physics-not-knobs, and graduated the Jul-14 inventory itself) and in good order, and the 2026-07-28 arc turned the instrument into a **closed loop**: instrument the settle (stage chains, world-time scrub `E`, region window to L14) → measure what it actually approaches (χ-shape criterion: one response time ≈ a quarter of the approach; at G=0 the criterion is exact, deposition carries the floor) → relocate the mystery the eye kept reporting twice over (no lakes is a **bed property**, not a water-kernel failure — and then, once the world was carved under both boundary contracts, not the tiling either: the epoch loop keeps Priority-Flood's raise, so the bed is depression-free by construction) → push structure one level deeper (tectonics is the column: sutures, zonation, uplift as the freeboard derivative — so present-anchor fragmentation is *youth*, truthful for a pre-collision era). The lies the loop caught are **the fill's kept raise, the tiling's grain-dependent crimes, and era**, not missing features; the ladder below is ordered by exactly that.

## What is true now, and measured

- **The core/view wall is a mechanism, not a discipline.** `Store::open_read_only` refuses puts with `PermissionDenied` and counts them; the explorer displays its own refused-write count. This closed a latent violation on `status`/`info`/`watch`, where `globe::render` reached a pull that computes and writes on a miss. `#form-core-view-wall` FE(6); `#form-store-as-save` FE(8) view half.
- **One view crate.** `crates/vivarium-explore`; `spikes/{globe,worldview}` and `slabs` / `vivarium-app` are in `archive/`. `vivarium explore` is a subcommand you type and a separate binary that runs, so Bevy never enters the world frame.
- **Deep time plays at the density you ask for**, by evaluating the cooling law at more temperatures — never by tweening. Nested bisection makes the view's ladder a strict superset of the builder's, bit-exactly, so coarser requests are 100 % store hits.
- **Two convergence gates are measured no-gos.** Water: no stationarity at the pinned timestep ( `#obs-water-fill-never-settles` ). Erosion: the residual is driver-bound and most tiles do no fluvial work at all ( `#obs-erosion-residual-is-driver-bound` ). The FE(4) claim stands; the *form* of each criterion is per-kernel and unearned.
- **A read coarser than the build level silently draws the uncarved prior** ( `#obs-coarse-view-draws-the-uncarved-prior` ), convicted by unit test. Found by the instrument, invisible to the eye.

## Open work, in rough order of what unblocks what

1. **Cross-tile base level** — and with item 1b landed it is now the *remaining* obstacle between the shipped tiling and its basins, not one of two. It buys: an un-starved incision driver (tile-local routing is 3.25× short of assembled routing on the same bed), the removal of seam pits (which rise monotonically as the grain is made finer), the capture-capable base level the χ criterion's form selection needs, and — newly — the inherited basins the bed can now retain but each tile still grades away at its own perimeter ( `#obs-lakes-are-routed-over-not-carved-away` FE(11)). **Its size is a strong function of grain against geography, measured on both sides:** at the L9 planet build the two contracts agree within 5 % at every rung of a 300-epoch history (1251 km tiles against a 279 km basin — the edges are never reached), while at the beacon footprint (78 km tiles, 313 km window) they end **259 m apart on the tile rings** and the tiled carve puts **63.5 %** of the window in closed depressions against the whole-window carve's single cell. So the repair is a *fine-grain* repair, and the honest planet-build justification is seam-free assembly, the χ criterion's form selection, and basin retention at the grain a viewer actually looks at. The repair still wants doing, and it wants doing *first*: the era work's chain-carried fork would multiply the rekey per cooling stage if it landed first. The kernel now carries `EdgeContract` — the two policies named, the geometric inference preserved as the default, two tests convicting it — which is what made the beacon comparison runnable at all; it is deliberately **not** yet in the complete key, since no keyed caller can select one. Neither contract is right at a coastless patch (one invents an outlet ring, the other invents a single outlet), so the answer there is a halo that exchanges with the neighbours the patch actually has.

   **1b. The fill repair — LANDED** ( `#obs-lakes-are-routed-over-not-carved-away` ). The epoch's Priority-Flood is now a routing surface the loop restores, so a closed basin survives the carve: a whole-face L9 control retains 0.47–0.51 % of its land in closed depressions (deepest 212–635 m) against 0.02 % before, the $\approx 2\times10^{13}\,\mathrm{m^3}$ per face the first epoch used to mint is never created, and four unit tests convict the two halves separately. Build cost is unchanged (12.0 s against 12.4 s for the L9 erosion sweep). Three physics decisions are declared rather than defaulted — no incision under standing water, lake trapping efficiency 1, talus and creep on rock — and the trapping efficiency is the least defended. **Open, and named:** the L11 and multi-face replications are not re-run; whether the $\varepsilon$'s directional artefact still reaches the bed is measured to persist on assembled reads but not isolated; and the shipped tiling still grades inherited basins away at every tile perimeter, which is item 1.
2. **Erosion at a cooling stage** — build the fluvial rung against colder cooling stages, where the assembled continents are. Designed, not built: `#form-erosion-at-a-cooling-stage` (the design) and `#obs-the-erodible-world-is-down-the-cooling-chain` (what is down there, measured). The present-anchor world's fragmented radial drainage is *physically truthful for its age*, and land triples with a kilometre of extra stand by the chain's cold end. Three things the earlier framing of this item assumed and the measurement corrected: emergence **saturates inside the authored chain** (extending it colder is not the win), the suture belts stay **thin** (0.16–2.82 % of the globe at the cold end — more land under weather, not an orogen to dissect), and the pour-grain ledger has **already removed** a fifth to a whole standard deviation of the stand a carried-forward bed would carve. That last one is why the iterated erode→rebound rung `#form-isostasy-column` FE(9) names is a *candidate replacement* for the ledger rather than a rung beside it — so the two compositions fork on physics, not cost. Cheapest honest first build: one cold stage, carved independently.
3. **The χ criterion's remaining opens** — the shape test is built and measured ( `#obs-chi-shape-is-erosions-criterion` : one $T_A$ ≈ a quarter of the approach; at $G{=}0$ the criterion is exact, so the floor is deposition; the per-cell form wins 1.31–1.52× once channels can cross a $U$ gradient). What ran closed the old items — including the steepening derivation, which returned a productive no-go (`msc/research-lem-sota/chi-steepening-derivation-2026-07-28.md`: classical $1{+}G$ is *forced* by the continuous equations, outlet export included, and the data reject it — the offset is kernel-structural, so no continuous algebra closes it). What remains: the **instrument path** to the rate gate (sediment-budget probe + hillslope-off control, on the post-fill-restore composition once it lands), χ under a *built* capture-capable contract (blocked on item 1), and a declared threshold (a nomos decision, not a probe's).
4. **The step-size question** (physics, Joseph's or a spike's) — whether `stable_dt`'s ceiling should scale with cell size. Blocks water's ε rung, and with item 1b landed it is now what stands between a bed that *can* hold water and water actually standing in it: `water-tile` settles 40 s of world time at any level ( `#obs-water-fill-never-settles` ), which would not fill a 200 m basin however good the bed is. This is the item to take next if the goal is a lake anyone can see.
5. **The demand spool** — the first standing beacon now lives in the manifest and the builder sweeps it; the spool proper (beacons → cones → work queue, multiplicity, per-beacon policy) is untouched. Regime G is the floor: "no demand" means spine-only, not idle.
6. **Store-backed navigation** — an explorer that upgrades continuously as memos land, rather than reading a census at open.
7. Standing, unowned: **the Caspian gap** — sea is elevation-thresholded, not connectivity-classified, so landlocked below-datum basins render as ocean and escape the depression census (Joseph's eye, first paint session; mechanism + repair direction in `#form-derived-sea-level` Working Notes; census probe is the convicting first step). **The msc/ graduation sweep** — per-item adjudication into core or `.super-archive/` (verify against the absorbing segment, never filename-match; `build-parameterization-findings` graduated 2026-07-29 after verifying all four items landed; the `research-lem-sota/` pair stays — actively cited by live segments as reference; spike dirs are the bulk of the remaining work, incl. the three self-destructing breadcrumbs on `#form-grid-equiangular-staggered` / `#detail-fvm-control-volume` / `#sketch-nomos-declaration-boxes`). A fresh-session job — do not start it under pending landings. Also: the stale-`src` GC; `globe::render` unaudited against `#norm-no-depiction-without-referent`; the source digest covering `src/bin/` (editing help text invalidates a world — Joseph's call) — the sidebar adds that it also does not cover rustc/LLVM version or FMA contraction. *(Zero inland standing water at L9 is no longer standing: probed, then split into three serial causes and two of them adjudicated — the fill is repaired (item 1b), the tile contract is item 1, and the water timestep is item 4.)*

## The rhythm this file protects

Claim or named gap in core → convict where possible → strengthen before soften → demote rival prose → name residuals honestly.

Two lessons from 2026-07-24 worth keeping in the rhythm. **The artifacts record what was decided; the transcripts record why.** And: **when the ask is for something visible, the visible thing is the deliverable** — a day of defensible layer-beneath work summed to a day where nothing on screen changed, and the telos ("session success is a truthful segment") governs what gets written down, not permission to leave the surface untouched.

*Last intuition pass: 2026-07-28.*
