# Consolidation status — big-picture intuition

*Body updated 2026-07-28 (late): the lede had lagged its own open list — caught by an outside reader, which is exactly the numbers-decay class `#norm-caught-disciplines-become-mechanisms` FE(2)(b) names. **Not claim canon.** When this file disagrees with `core/`, core wins.*

## The picture, in one paragraph

The claim channel is **92 segments** and in good order, and the 2026-07-28 arc turned the instrument into a **closed loop**: instrument the settle (stage chains, world-time scrub `E`, region window to L14) → measure what it actually approaches (χ-shape criterion: one response time ≈ a quarter of the approach; at G=0 the criterion is exact, deposition carries the floor) → relocate the mystery the eye kept reporting twice over (no lakes is a **bed property**, not a water-kernel failure — and then, once the world was carved under both boundary contracts, not the tiling either: the epoch loop keeps Priority-Flood's raise, so the bed is depression-free by construction) → push structure one level deeper (tectonics is the column: sutures, zonation, uplift as the freeboard derivative — so present-anchor fragmentation is *youth*, truthful for a pre-collision era). The lies the loop caught are **tiling and era**, not missing features; the ladder below is ordered by exactly that.

## What is true now, and measured

- **The core/view wall is a mechanism, not a discipline.** `Store::open_read_only` refuses puts with `PermissionDenied` and counts them; the explorer displays its own refused-write count. This closed a latent violation on `status`/`info`/`watch`, where `globe::render` reached a pull that computes and writes on a miss. `#form-core-view-wall` FE(6); `#form-store-as-save` FE(8) view half.
- **One view crate.** `crates/vivarium-explore`; `spikes/{globe,worldview}` and `slabs` / `vivarium-app` are in `archive/`. `vivarium explore` is a subcommand you type and a separate binary that runs, so Bevy never enters the world frame.
- **Deep time plays at the density you ask for**, by evaluating the cooling law at more temperatures — never by tweening. Nested bisection makes the view's ladder a strict superset of the builder's, bit-exactly, so coarser requests are 100 % store hits.
- **Two convergence gates are measured no-gos.** Water: no stationarity at the pinned timestep ( `#obs-water-fill-never-settles` ). Erosion: the residual is driver-bound and most tiles do no fluvial work at all ( `#obs-erosion-residual-is-driver-bound` ). The FE(4) claim stands; the *form* of each criterion is per-kernel and unearned.
- **A read coarser than the build level silently draws the uncarved prior** ( `#obs-coarse-view-draws-the-uncarved-prior` ), convicted by unit test. Found by the instrument, invisible to the eye.

## Open work, in rough order of what unblocks what

1. **Cross-tile base level** — and it is now known what it does and does not buy, because the world has been carved both ways ( `#obs-fill-writes-itself-into-the-bed` ). It buys: an un-starved incision driver (tile-local routing is 3.85× short of assembled routing on the same bed), the removal of seam pits (which rise monotonically as the grain is made finer), and the capture-capable base level the χ criterion's form selection needs. It does **not** buy lakes — that attribution has moved to item 1b. **And its size is a strong function of grain against geography, now measured on both sides:** at the L9 planet build the two contracts agree within 10 % at every rung of a 300-epoch history (1251 km tiles against a 279 km basin — the edges are never reached), while at the beacon footprint (78 km tiles, 313 km window) they end **280 m apart** and the tiled carve puts **60.6 %** of the window in closed depressions against the whole-window carve's ~0. The earlier 13 % figure understated it because that reader had edge sinks of its own. So the repair is a *fine-grain* repair, and the honest planet-build justification is seam-free assembly and the χ criterion's form selection rather than visible basins. The repair still wants doing, and it wants doing *first*: the era work's chain-carried fork would multiply the rekey per cooling stage if it landed first. The kernel now carries `EdgeContract` — the two policies named, the geometric inference preserved as the default, two tests convicting it — which is what made the beacon comparison runnable at all; it is deliberately **not** yet in the complete key, since no keyed caller can select one. Neither contract is right at a coastless patch (one invents an outlet ring, the other invents a single outlet), so the answer there is a halo that exchanges with the neighbours the patch actually has.

   **1b. The fill writes itself into the bed** — the actual lake blocker, and newly separable. `Fluvial::erode` keeps Priority-Flood's raise where the three readers restore it, so the stored bed is depression-free by construction: the uncarved prior holds 2.3–2.65 % of its land in closed depressions ($\approx 2\times10^{13}\,\mathrm{m^3}$ per face) and one epoch leaves 0.12–0.14 %. The repair (route on the filled surface, incise on the unfilled one) is standard in the LEM literature, is a real physics change rather than a bug fix, and is neither built nor costed.
2. **Erosion at a cooling stage** — build the fluvial rung against colder cooling stages, where the assembled continents are. Designed, not built: `#form-erosion-at-a-cooling-stage` (the design) and `#obs-the-erodible-world-is-down-the-cooling-chain` (what is down there, measured). The present-anchor world's fragmented radial drainage is *physically truthful for its age*, and land triples with a kilometre of extra stand by the chain's cold end. Three things the earlier framing of this item assumed and the measurement corrected: emergence **saturates inside the authored chain** (extending it colder is not the win), the suture belts stay **thin** (0.16–2.82 % of the globe at the cold end — more land under weather, not an orogen to dissect), and the pour-grain ledger has **already removed** a fifth to a whole standard deviation of the stand a carried-forward bed would carve. That last one is why the iterated erode→rebound rung `#form-isostasy-column` FE(9) names is a *candidate replacement* for the ledger rather than a rung beside it — so the two compositions fork on physics, not cost. Cheapest honest first build: one cold stage, carved independently.
3. **The χ criterion's remaining opens** — the shape test is built and measured ( `#obs-chi-shape-is-erosions-criterion` : one $T_A$ ≈ a quarter of the approach; at $G{=}0$ the criterion is exact, so the floor is deposition; the per-cell form wins 1.31–1.52× once channels can cross a $U$ gradient). What ran closed the old items; what remains: a derivation of the detachment+deposition steepening (would make the rate half a gate — the measured offset refuted $1{+}G$ and the tidy $1{+}G/2$ is labeled underived), χ under a *built* capture-capable contract (blocked on item 1), and a declared threshold (a nomos decision, not a probe's).
4. **The step-size question** (physics, Joseph's or a spike's) — whether `stable_dt`'s ceiling should scale with cell size. Blocks water's ε rung — though note the no-lakes diagnosis moved to item 1b's kept fill; the two causes are independent and both sufficient.
5. **The demand spool** — the first standing beacon now lives in the manifest and the builder sweeps it; the spool proper (beacons → cones → work queue, multiplicity, per-beacon policy) is untouched. Regime G is the floor: "no demand" means spine-only, not idle.
6. **Store-backed navigation** — an explorer that upgrades continuously as memos land, rather than reading a census at open.
7. Standing, unowned: the stale-`src` GC; `globe::render` unaudited against `#norm-no-depiction-without-referent`; the source digest covering `src/bin/` (editing help text invalidates a world — Joseph's call) — the sidebar adds that it also does not cover rustc/LLVM version or FMA contraction. *(Zero inland standing water at L9 is no longer standing: probed, and homed at `#obs-fill-writes-itself-into-the-bed` after the whole-face control moved the attribution off the tiling. What that opens instead is item 1b, and the fact that a boundary-contract repair will not by itself put water anywhere.)*

## The rhythm this file protects

Claim or named gap in core → convict where possible → strengthen before soften → demote rival prose → name residuals honestly.

Two lessons from 2026-07-24 worth keeping in the rhythm. **The artifacts record what was decided; the transcripts record why.** And: **when the ask is for something visible, the visible thing is the deliverable** — a day of defensible layer-beneath work summed to a day where nothing on screen changed, and the telos ("session success is a truthful segment") governs what gets written down, not permission to leave the surface untouched.

*Last intuition pass: 2026-07-28.*
