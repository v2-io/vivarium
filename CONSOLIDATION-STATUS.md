# Consolidation status — big-picture intuition

*Body replaced 2026-07-24 (its own protocol: replace when the picture changes a lot). **Not claim canon.** When this file disagrees with `core/`, core wins.*

> [!warn] **Stale as of `37b3987` (2026-07-24 evening) — the body below predates a large same-day wave and a full replacement is the recommended next-session opener.** What that wave landed (all on `main`, see `git log` + the DECISIONS 2026-07-24 tail): source-derived nomos keys (every key folds a whole-crate source hash); the ordinum maturity ladder shown in `vivarium status` + bin-level admission tests; fan-path true great-circle lengths; the router pricing verdict (length fixed, tree-diagonal consequential); the emerged-land Record predicate; mean-pin **retired** (deletion measured-best); the mantle-thermal **nomos** (land emerges along a cooling trajectory); the rock-mass **ledger** built **and adopted as the live surface** (rebound bounds relief, mass conserved ~1e-12); craton **nucleation-and-growth** replacing fBm speckle (few coherent cratons that accrete as the mantle cools); deep-time **playback** on the globe (press **T**); epoch surfaces as **store citizens** (warm-process pour cost → 0); the closure **no-go** to the Appendix (`#obs-coarse-only-closure-nogo`); and two decided laws — **stage is a marker not a gate** and **memoized means store object** (`#form-store-as-save` FE(6) definitional). Nearly every "open" bullet below (mantle-thermal, rock-mass, router priority) is therefore already superseded by core — trust `core/` + `git log`, not this body, until it is replaced.
>
> **The one genuinely-open thread from the wave:** the **era-sharpening timing clause** into `emerged_land_verdict` — the temporal `NotPredicable` clauses of the emerged-land predicate, to be wired against the *post-ledger, post-nucleation* cooling trajectory (the agent that held it was interrupted before delivering; it deliberately waited for the ledger + nucleation to land so it would predicate the honest trajectory, which they now have). Clean next-session head. Other named-but-not-started items: craton **shape anisotropy** (Joseph noted the cratons look abnormally circular — v1 grows each radially from one nucleus; real cratons are sutured multi-terrane aggregates, declared-open, the shape rung of the same law); **epoch densification** (playback shows 6 materialized epochs — the ladder grain is a cheap dial, near-free once epoch surfaces are store-warmed); and the store-wide **GC** of source-hash-superseded generations.

**The prose mine is closed.** The design/theory thick middle (`DESIGN-REDUX`, `DESIGN-MATERIAL`, `DESIGN-SYSTEMS`, `NOMOS-CONTRACT`, `ARCHITECTURE`, the multiscale theory pair, plans, toolchain) is fully adjudicated into `core/` and graduated to `.super-archive/` — do not re-mine ice for claims. Live `doc/` is essentially `PROCESS.udon` (a process instrument, not a mine). `audits/` is empty of pending reports.

**Claim channel:** `core/OUTLINE.md` (79 segments, organized by subject per its Organizing principle) · `core/src/` · `#scope-segment-canon`. **Open work is the OUTLINE's named gaps plus each segment's Working-Notes residues** — the honest backlog, no longer a forecast:

- §VII field nomos by family: **isostasy column v1 live** (`#form-isostasy-column` — Claimed, not Kept) + **erosion composition** (`#detail-erosion-composition`); still open — mantle-thermal *nomos*, water kernel, climate, rock-mass return; Biosphere all-gap. Mines: `tabularium/terrestris.ordinum.udon`, `ASSUMPTIONS.md`, `msc/spike-principled-router/`, `ref/research/` surveys.
- §VII/§IV: **Abyssal phase home piloted** (`#detail-phase-abyssal`); remaining phases + ordinum ontology beyond the flux join.
- §I optional: ETHICS slices; strengthen-before-soften; epistemology ladder.
- On-host residues: column statistic fork, NomosDecl earning procedures, router successor, three-scoped unbuilt pieces.
- Cross-cutting discipline: promotion passes up the stage ladder (everything sits at `draft`).

**Rhythm this file protects (unchanged):** claim or named gap in core → convict where possible → strengthen before soften → demote rival prose → disposition audits with residuals named. Session success is still a moved segment or a convicting instrument — not an update to this file.

*Last intuition pass: 2026-07-24.*
