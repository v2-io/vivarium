# Adjudication — process/one-off family (msc residue vs the claim core)

*2026-07-29. Adjudication only; nothing edited, moved, or deleted. Every verdict below was checked against absorbing-surface **content** (core/src, DECISIONS, LEXICON, ASSUMPTIONS, FORMAT, MANIFEST, live code/tests), not filename match. DECISIONS was treated as first-class absorbing surface per your mid-flight note — it mattered in three places, flagged inline.*

## Verdict table (one line per item)

| Item | Verdict |
|---|---|
| `msc/claim-channel-cleanup-2026-07-23.md` | **graduate-clean** — every demotion target since graduated wholesale (MANIFEST batches 5–8; `doc/` is PROCESS.udon only); its three "judgment calls not made" all mooted or landed (`#detail-regula-design` exists) |
| `msc/promotion-mine-2026-07-23-continuity.md` | **graduate-clean** — mirrored line-for-line by MANIFEST's "Provenance audit 2026-07-23" table; all four REDUX landings verified live as segments; the PHASES-pin honest residual is carried in MANIFEST |
| `msc/harness-reconciliation-2026-07-24/` (8 files) | **graduate-as-record after landing the Tier-1 residue below** — the bulk is absorbed (census refresh 07-24 evening names this audit; `#form-time-indexed-stage-chains` is Component E built-and-convicted; both second opinions live near-verbatim in `#form-complete-content-addressed-key` KI(3) and `#scope-segment-canon`; `#norm-instrument-before-tuning`; FORMAT §5.1 + `bin/check-decision-refs`), but it still holds the only carrier of several unlanded items |
| `msc/audit-onboarding/confusion-log.md` | **graduate-as-record** — all eleven confusions (C1–C11) verified fixed by the re-founding or since (LEXICON `manifest` entry exists; `regula` is `:status open`; ASSUMPTIONS de Almeida–Bates row corrected 07-13; NomosDecl boxes landed 07-24; the `:supersedes` header-line defect fixed at line 357). Historical value: the measured-onboarding-cost specimen behind `#norm-caught-disciplines-become-mechanisms`'s lineage |
| `msc/audit-onboarding/drafted-patches.md` | **graduate-clean** — none applied, none needed: every target file (CLAUDE/ORIENTATION/ARCHITECTURE of that era) was superseded wholesale by the re-founding; the mean-pin issue got its own segment + executed retirement (`DECISIONS[mean-pin-retirement-…]`) |
| `msc/audit-onboarding/nomos-design-sketch-atmospheric-circulation.md` | **graduate-as-record with one pointer landed** — see Tier-2 #1 (`promise[coriolis]` still has no `:kept-by` / `|predicate`; this file is the only design for its keeper) |
| `msc/design-convictable-declarations/README.md` | **graduate-after-landing-X** — the breadcrumb's mapping is **wrong** (verified, detail below); X = re-point + delete the breadcrumb per its own instruction, and record the assumptions reverse-guard residue (Tier-1 #6) |
| `msc/reflections/compaction-fidelity-modes.md` | **(d)-with-a-hook**: cross-project methodology whose route-out is still unexecuted. Recommend homing the obligation exactly the way `DECISIONS[trace-vestigia-obligation-homed-in-global-memory]` (07-29) just did for the same shape of problem, then graduate. If lifted: it uses the retired "Three Deaths" name — update per global memory before propagating |
| `msc/agent-briefs/` (14 files) | **split**: graduate the nine 07-23/24 adjudication briefs/reports as records (verdicts verified executed or honestly escalated — trail below); the four 07-28 coordination notes are absorbed records (measurements verified in segments); **keep the directory live** — it is still receiving coordination notes (three landed 07-28) and `DECISIONS` `|impact` rows cite into it (e.g. entry 1247 cites `l13-patch-coords.md`). The dir is not a pattern-library (the pattern lives in AGENTIC-DELEGATION + project memory); its README's standing corrections are absorbed into live `CLAUDE.md` (code-rank rule 7, super-archive scope) |

## The two verification asks you named

**1. The `#sketch-nomos-declaration-boxes` breadcrumb — mapping does NOT hold.** The note claims `msc/design-convictable-declarations/` is "the design work behind boxes ②–⑤." It is not: the README is asks 4/5 — source-derived versions (A), declaration-derived keys (B), assumptions reverse-guard (C). Zero of its content is about geometry/semantics/structure/claim boxes. The actual absorbing surfaces, verified clause-by-clause:

- **Sketch A (source-derived version): absorbed and superseded-by-stronger.** `#form-complete-content-addressed-key` FE(4): whole-crate `VIVARIUM_SRC_HASH` via build.rs, folded by `NomosDecl::key`, probed both directions (`every_nomos_key_folds_the_source_hash`, `injected_source_hash_matches_live_source`, nomotheke.rs:931–968). Human-readable stem preserved exactly as the README asked (hand-stamped version stays as the legible era). Piquant detail: what shipped is the README's **option 2**, which it called "likely a no-go" — while its preferred option 1 (per-module) became the **measured no-go** (KI(3), 2026-07-24). A clean pre-registered-miss specimen.
- **Sketch B (Merkle / declaration-derived keys): refuted by measurement, refutation homed.** KI(3)(a): `deps` is hand-maintained and under-covers (`MANTLE_THERMAL.deps` empty over a five-module computation) so declaration-derived attribution under-keys — the unsafe direction; (b) even the compiler-safe module-graph form spares 0 of 39 commits. The README's own "cheap intermediate" (dep-identity-in-key assertion) exists: `Key::with_dep_versions` + the KI(2) pins. The measured 07-21 NOISE under-keying defect is closed by SRC_HASH (any source edit invalidates the store); the specific measurement's only carrier is this README — fine as frozen provenance.
- **Context note (asf pointwise-on-keyed-draws): absorbed** at `#post-determinism-as-ontology` line 35 ("no distributional equivalence test can license cache reuse… now mechanized by the source digest"), homed there at batch-10 graduation of `feedback-from-asf.md`.
- **Open q. 3 (worldview cache): mooted** — the spike moved to `archive/worldview-spike/`; ASSUMPTIONS.md:87 records `FILL_ALGO_VERSION` as "retired by source-derived versions."
- **NOT absorbed — Sketch C, items 1–2** (Tier-1 #6 below).

So: execute the note's own re-point-or-delete instruction with the correct pointers (`#form-complete-content-addressed-key`, `#post-determinism-as-ontology` — not the boxes sketch), land Tier-1 #6, then graduate and delete the note.

**2. `compaction-fidelity-modes.md`.** The prior flag is accurate: the header still carries the unexecuted lift ("candidate for lift into global memory-curation… a future curator or the memorata agent should elevate it"). Nothing in project memory, global memory index, or DECISIONS executes or supersedes it. The content (three sight-type failure modes, falsifiable-prediction-before-recontact, inherit-map-not-mood) is genuinely not duplicated anywhere I can see — the global index's `[[compaction-failures]]` is a different, "partly outdated" reference note. Since super-archive is treated-as-deleted, graduating it un-homed would bury live cross-project methodology. Honest disposition: this is exactly the `trace-vestigia` shape — a vivarium-resident obligation with a non-vivarium home — and the mechanism for resolving that shape was exercised two entries ago in DECISIONS. Home it (or a pointer-obligation) globally, then graduate. One correction if lifted: "the Three Deaths" is a retired name (global CLAUDE.md: four deaths, do not repropagate outside historical notes).

## Residue — Tier 1 (real: measurement/decision/design whose only carrier is msc or ice, or a live core defect this family convicts)

1. **Restart-in-place + fork/shared-object-pool.** Only carriers: `.super-archive/from-plan/builder-explorer-decoupling.md:29` + this audit. `#form-builder-admission` KI(2) points at `#detail-builder-daemon`, which does not contain it — a dangling pointer flagged 07-24, still dangling. The shared-object-pool clause (seed-in-every-key ⇒ forks cannot alias ⇒ free cross-fork dedup) is a real design fact with zero live carrier.
2. **Twice-flagged stale segments, still stale:** `#form-in-vivia-citation` ES(2) and `#form-add-system-contract` FE(4) both call source-derived versions "the target" while `#form-complete-content-addressed-key` FE(4) says wired-and-probed. Flagged in two slice reports (retired-front-doors §5.1, toolchain §4.2); unfixed as of tonight. One-line fixes; the citation segment is the one that gets quoted outward.
3. **`#detail-abyssal-parity-build` FE(4) Phase-1** still states "probe: edit kernel invalidates only dependent cone" unqualified — now contradicted by KI(3)'s measured no-go. Neither cites the other (FINDINGS §5.2's residue).
4. **`#form-store-as-save` FE(8)** still says "Phase-0 convention + provisional banners are present practice" on the builder side — the run-modes adjudication convicted this exact conflation (the Phase-0 *banner* was never implemented; the *roots flag* is the flux waiver, a different predicate). The adjudication asked for the non-identity to be stated; the view-half mechanization was added instead. Half-done.
5. **GC of superseded-by-SRC_HASH generations** — named open in `DECISIONS[epoch-surfaces-are-store-citizens]` `|impact` and CONSOLIDATION-STATUS; absent from `#disc-open-problem-census`, whose FE(1) purpose is to carry exactly this. (GC and restart-points are one question seen twice.)
6. **Assumptions reverse-guard (README Sketch C, items 1–2).** The live guard (`every_assumption_is_in_the_ledger`, nomotheke.rs:898) is still declared→ledger only and still `LEDGER.contains(a)` substring. Ledger→declared (unclaimed-row detection) and row-match tightening are unbuilt **and unrecorded** — no census row, no segment WN, no DECISIONS entry. The README also carries the forcing-function argument (ASSUMPTIONS wants udon; the parser should read data, not prose) — worth carrying with it.
7. **`ASSUMPTIONS.md:70` is stale against core** (adjacent catch, this family's read convicted it): the "rock-mass conservation — NOT enforced, no global ledger" row predates the 07-24 rock-mass ledger (`#form-isostasy-column` FE(9), `rock_mass_is_conserved_across_the_ledger_pass`). The honest row now is grain-split: pour-grain conserved-and-convicted; tile-grain outlet export still un-ledgered (the FE(9)/boundary-contract open rung). ASSUMPTIONS is a FE(3) carve-out authority — staleness there is worse than in prose. (Its row is also mis-columned in the table it sits in.)
8. **Manifest-template rigor-floor line-to-hold** — "rigor-floors live on the manifest/template, never baked into the ordinum; that's the line to hold if we collapse" (VIVARIA-DEFINITIONS:388, verbatim in retired-front-doors §1.3). The collapse happened (`#form-manifest-prescribes-vivium`), the line did not travel. One clause on that segment closes it.

## Residue — Tier 2 (arguable: judgment calls, wishes, or items with partial/adjacent carriers)

1. **`promise[coriolis]` keeper design** — the ordinum promise still has no `:kept-by` and no `|predicate` (verified, terrestris.ordinum.udon:144); the atmo-circulation sketch's zero-spin-control predicate proposal is the only existing design for it. Proposal-grade, 07-13-era codebase; worth a one-line pointer (ordinum comment or census row) before its carrier graduates, not a segment.
2. **DECISIONS read-back layer** — the decision-representation recommendation partially landed (FORMAT §5.1 citation form + `check-decision-refs`, both dated 07-24); the ~50-line projection, the four-species-no-citation guidance, and owner/cadence remain open and are Joseph's (FINDINGS §7 says so). `DECISIONS[ascii-globe-in-info-colored-by-state]` (`:by joseph :status wish` — the space half delivered in `vivarium info`, the time/phase half needs Component E, which now exists) and `[new-system-must-reach-the-goal-not-reimplement-poc]` (`:by joseph :status decided`) remain uncited by core.
3. **Explorer intents** (Joseph 2026-07-02, twelve, ice-only): one ("steerable time regimes") is now a named OUTLINE §III gap row with attribution; the other eleven have no live carrier. Partly re-derivable from physics segments; as *design intents that bind the explorer* they exist only in ice.
4. **Elastic logical time** — "a pure-agent world has fully elastic logical time" (ARCH §4's second half); core kept only the ~2 Hz human half. One sentence; it is the definition of "background" building.
5. **Execution-class scheduling semantics** ("preemptively, rarely, arbitrarily slow because checkpoints amortize to zero" + the 07-03 symptom list) — names and coupling purpose live (`#form-fidelity-ladder` FE(5), NomosDecl fields); the scheduling law a daemon would be built on is ice-only.
6. **Global-confidence-scalar rejection** ("collapses independent axes into a lie; the honest object is the per-promise per-cone fold") — a HUD/status design rule with no live carrier; exactly the surface a build-watching HUD invites.
7. **Declared-vs-audited discrepancy flag** (a *report*, distinct from the correctly-refused agentic-verdict-as-store-truth) — absent, possibly by conflation with the refusal; needs a deliberate keep/drop, not silence.
8. **Phase-transition-IS-the-citable-artifact** identity — ingredients in `#detail-vivium-lifecycle` FE(2)/(4) and `#form-in-vivia-citation`; the identity itself unstated.
9. **Mourning×GC conjunction** — both halves live (`#detail-vivium-lifecycle` doctrine 7; `#form-store-as-save` KI(2)+WN); the tripwire sentence ("the moment eviction lands, an ethical invariant needs a mechanism that does not exist") is joined nowhere.
10. **Toolchain ops residue** — cargo-mutants `--package` flag, proptest-regressions-persist-seeds caveat, the `inari` interval-arithmetic successor path for `Quantity`: code-comment/ice-only. Promote when `bin/check` is next touched.
11. **Catalyst-loop owner** — `#norm-declared-violation-is-not-license` WN and `#norm-probe-sensitivity` WN still point at each other; neither owns "budget for the work an honesty mechanism surfaces."
12. **Watchpoint** — LEXICON entry still absent (recorded honestly in OUTLINE gap row 131 and stage-chains WN, so this is absorbed-as-gap; listed only because the *trigger* semantics — writer-fired event vs standing query — live in ice alone).
13. **Fable-middle-peel F1–F9 disposition** — never verified (adjudications §5's own open row). Mostly mooted: the targets graduated wholesale, and dual-home defects frozen into treated-as-deleted ice are inert; the six live-core segments it audited have since been through further waves. One `git log` on the pre-graduation range would settle the residue; low stakes.
14. **The 07-21 measured NOISE defect and the sea-level counterexample runs** — the defects and their refutations are homed (KI(2)/(3)); the raw measurements' only carriers are this family's files, which is the right place for them as frozen provenance once graduated.

## What is definitively absorbed (spot-verified content, no action)

Component E → `#form-time-indexed-stage-chains` (built, convicted, first-in-field restart-equivalence test); the round-trip probe → `#form-depend-by-key-never-latest` (built *with sensitivity conviction* — injected fault, probe hardened, re-injected fault fails); complete-key Discussion self-contradiction → fixed (cone property now qualified by source-component); the FE(4)-has-no-convictor finding + loss list → `#scope-segment-canon` line 49; "findability and convictability are one axis" → `#norm-caught-disciplines-become-mechanisms` (its whole thesis) ; run-modes tripwire → fired 07-28 and partially mechanized (`Store::open_read_only` view-half; near-misses in project memory); store-as-bus evidence → superseded-by-stronger (07-28 four-writer measured day); bench/noise-floor/one-variable → `#norm-instrument-before-tuning`; fitness-vs-declared-prior → practiced (χ-criterion, emerged-land literature bands, pre-registered changelog misses) and named in DECISIONS:525 ("It has never been used. This is what it was built for" — now used); sibling-world counterfactuals → `#post-determinism-as-ontology` (theorem-grounded, stronger than the ice derivation); both 07-28 brief measurements (0.0–0.62% waterline coupling; precip_weight uniform-rain trap; 3.25× assembled trunk) → verbatim in `#form-erosion-at-a-cooling-stage` FE(8), `#obs-the-erodible-world-is-down-the-cooling-chain` FE(7), `#obs-tile-outlets-grade-away-the-basins` FE(4)/WN.

## Adjacent observations

- **The self-destructing-breadcrumb pattern worked** — but note it nearly misfired: the from-memory mapping was wrong, and a graduation sweep that trusted it would have "verified" absorption against the wrong segment. The note's own "asserted from session memory, not verified" honesty label is what saved it. Worth keeping that label mandatory on future breadcrumbs.
- **OUTLINE-row vs segment-body drift** is a live failure mode with two specimens in one day (the fill-restore brief fixed one; gap row 131 was updated same-day). A row is what readers meet first; nothing convicts a stale row — same shape as `#norm-caught-disciplines-become-mechanisms`'s "measured numbers went stale within hours."
- `bin/check-decision-refs` deliberately excludes `msc/` and `.super-archive/` — graduating these files keeps the linter's scope-map consistent; no ref repairs needed on graduation.
- The harness-reconciliation FINDINGS' method notes both landed in project memory (`transcripts-record-why`; the term-list-inventories-inherit-one-vocabulary caution is live practice in the multi-agent memory) — nothing further to lift from them.

*Staying on the line for merge follow-ups.*
