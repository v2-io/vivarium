# Router/grid family — msc adjudication (agent slice)

*Adjudicated 2026-07-28 against segment content + DECISIONS + census, not filename-match. No files moved, nothing edited. All paths absolute-from-repo-root (`~/src/arch/vivarium/`).*

**One framing note before the verdicts, because it changes how they read:** for this family the deep absorption surface is **DECISIONS**, not the segments. The four council-accepted entries (`the-router-is-a-scalar…` L468–485, `mfds-output-is-not-a-discharge…` L487–501, `routing-spiral-landscape-consequence…` L1039–1048, `corrected-scheme-is-affordable-via-precomputed-stencil` L759–777) carry the spikes' measurements at near-verbatim fidelity — arm tables, refuted hypotheses, limits, even the spikes' own named residue. The segments (obs-routing-curl-spiral FE(5)–(8), obs-cube-locked-kernel-bias, census row) are correct summaries pointing at those entries. A sweep that counted only segment text as "absorbed" would wrongly find large unlanded meat here; counting DECISIONS measured entries as absorbing surface (which the repo's own claim-canon table licenses for "who decided what" + measurement trail), the family is close to clean.

## Verdicts (one line each)

| dir | verdict |
|---|---|
| `msc/spike-curl-probe/` | **graduate-after-repointing** — fully absorbed; two live citations in core point at it |
| `msc/spike-router-pricing/` | **graduate-after-repointing** — fully absorbed (DECISIONS L1040); fix the stale contradiction it exposes in `obs-routing-curl-spiral` when landing |
| `msc/spike-principled-router/` | **graduate-after-landing-X** — two real unlanded items (below) + relocate the `refs/` PDFs rather than bury them |
| `msc/spike-corrected-scheme-cost/` | **graduate-after-repointing** — fully absorbed (DECISIONS L759–777, incl. its own residue); its breadcrumb's mapping is wrong |

## Breadcrumb verification (the explicit ask)

**1. `obs-routing-curl-spiral` Working Notes L43 (names `spike-curl-probe/`): mapping HOLDS.** Every claim in `PREDICTIONS.md` (identity restatement, D4 null-test theorem, level-independence, quadrature-error trap, honest-negative shape) is present in the segment FE(1)–(4) + FE(3), and in DECISIONS `routing-violates-the-potential-identity…` (L417+). The file's remaining value is provenance (the pre-registration artifact) — exactly what `.super-archive/` preserves. Before deletion of the note: the segment cites the spike path **twice outside the breadcrumb** — Epistemic Status L29 and Working Notes L39 ("Instruments") — both need re-pointing to the `.super-archive/` path or to the harness (`crates/vivarium-world/examples/curl_probe/`, which is live and carries the identity restatement in its module doc).

**2. `form-grid-equiangular-staggered` Working Notes L46 (names `spike-principled-router/` + `spike-router-pricing/`): mapping is WRONG as stated, though no content is orphaned by it.** The grid segment owns only the *open-router clause* (FE(4)) — it carries none of the router measurements. The actual absorbing surfaces are:
- `core/src/obs-routing-curl-spiral.md` FE(5)–(8) (scalar/vector dissolution, remedy stack a–d, pricing verdict);
- DECISIONS L468–485 (①–⑦: reconstruction identity 2nd-order on spherical cells; p=1 four-line theorem + the 0.24° "intrinsic baseline" falsification; second-moment refutation with R²≈0.06; the wrong-coordinates dissolution; 47.8% phantom flux / diagonals make MFD not-an-FV-scheme; rotation test PASSES ≤1.42% pk-pk; level-dependence table) and L487–501 (boundary-integral finding, TPFA/orthogonality split, GMS subsumption);
- DECISIONS L1039–1048 + census row (`disc-open-problem-census.md` L44) for the pricing spike.
So: re-point the breadcrumb's "likely primary absorbing segment: this one" to those homes before deleting it. The MEASUREMENTS.md tables are mirrored in DECISIONS ⑤⑥⑦ down to individual numbers (20.76/5.17, 0.52 vs 0.984, 5.17→4.73 flat); `RESULTS.md`'s three-part decomposition and CUBE table are verbatim in L1042.

## Real unlanded meat (tier: real)

1. **Pits/flats/accumulation zones are outside Coatléven's well-posedness theory — and Priority-Flood manufactures them.** `msc/spike-principled-router/DERIVATION.md` §6, last bullet ("his §4: the discrete tell is `s_K = 0`. They are not a corner case in a real LEM, and Priority-Flood is *how we make them*"). Grepped `core/src/` + DECISIONS: nowhere. This is a standing *limit on the owed FE(6c) remedy* — the census row and `obs-routing-curl-spiral` FE(6)(c) both say "Coatléven vector reconstruction is the owed next step" without this caveat. It should ride wherever that owed-next-step lives (FE(6) remedy stack or the census row), or the FE(6c) pricing spike will rediscover it the hard way.

2. **Coatléven 2020's actual theorems, and a stale "unread" marker.** DECISIONS L501 `|ref` marks Coatléven 2020 "[⊘ unread]", but `DERIVATION.md` §7 read it primary: **Thm 6.1 / Cor 6.2 — the flux-vector estimate converges in L² at rate h^{1/2} (not O(h)), under mesh regularity (A1) and a *strongly consistent gradient reconstruction operator* (Def 4.2).** Two consequences only the spike carries: (a) the quantitative convergence expectation for the FE(6c) pricing is h^{1/2}, and (b) Def 4.2 makes remedy step (d) (the non-orthogonality-corrected gradient) a stated *precondition* of step (c), not merely "a separate error separately fixed" as FE(6)(d) currently reads. The "[⊘ unread]" is also simply false now and should be corrected (the PDF is in-repo, see rubble note below).

## Arguable residue (tier: arguable)

3. **The free identifiability probe.** DERIVATION §4.3 derives from the paper that a *uniform* `k_m` (Manning-type conveyance) and any `η_w` are structurally inert on routed flow, and specifies the one-run probe: "perturb a uniform `k_m` and assert the routed field is bit-identical — a probe that must return no change," framed as exactly the inert-undeclared-parameter class the nomotheke exists to catch. DECISIONS L484/L497 carry the identifiability *claim* as "worth confirming, may retire parameters" — the probe design and the nomotheke framing live only in the spike. Cheap to land as one line wherever the confirm-item lives.

4. **DERIVATION §5.4's open question** — is D-∞ bad because its dispersion is too low or because its 8-facet direction estimate is biased ("separable and nobody has separated them"; the E2 oracle experiment is named as next). Plausibly *superseded* by the scalar/vector dissolution + remedy stack (Coatléven yields direction and magnitude from one object, mooting the moment-design family), and MEASUREMENTS §2's ORACLE rows (oracle-ψ moves sca_err only 5.17→5.16) bear on it — but no core text adjudicates it. Either land the supersession sentence or the question is unowned. I lean superseded; that's a judgment, not a verification.

5. **Does any *segment* own the scheme-cost result?** The affordability measurement (+2.7%/epoch precomputed; 790× as-written is a ratio on 0.24% of the epoch; frozen stencil leaks conservation; f64-build/f32-store; SoA layout; **halo 1→2** seam cost unpriced; cache cliff at N≈437 with 64² tiles 50× below it) lives in DECISIONS L759–777 and inside the grid decision's `|reason` (L510) — no segment carries it. `detail-structure-scheme-map` (the breadcrumb's claimed absorber, L61) has only the symmetry row; `form-grid-equiangular-staggered` FE(3)/Status never mentions cost; the census row doesn't carry the two open items the DECISIONS entry itself names (⊘ **MFD's two-pass routing cost NOT measured — "the obvious next spike"**, L773; halo 1→2, L772). If DECISIONS-as-carrier suffices (my read: yes for the measurement; shakier for the *open items*, which the census exists to index), then only the census gains a line; your call.

## Safely graduatable rubble

- `spike-corrected-scheme-cost/stencil_bench.out` — raw output, reproducible (`examples/stencil_bench.rs` is live in-repo); every number and both refuted hypotheses are in DECISIONS L759–777. Pointer to fix: DECISIONS L777 + L516 `|ref` cite the msc path.
- `spike-router-pricing/{RESULTS-run,RUN-1,RUN-2}.txt` — raw logs; harness live (`examples/router_pricing.rs`). `NOTE-to-coordinator.md`'s open offers (memoize, seed sweep, FE(6c) build) are recorded as owed/limits in DECISIONS L1045; its referenced `DECISIONS-draft` correctly no longer exists in the dir (landed as L1040). Pointer to fix: DECISIONS L1048 `|ref`.
- `spike-principled-router/refs/` (~48M, the bulk of the family's size) — **do not bury; relocate.** Contains the paper-of-record PDFs: Coatléven 2020 M2AN (the theorems the FE(6c) work needs — possibly its only in-repo copy; DECISIONS points at `ref/research/pdfs/markdowns/CorrectedMFD/` for the 2025 paper only), the 2025 HAL preprint of record, esurf-2024. Suggest `ref/research/pdfs/` (and/or relata) so the owed pricing work can reach them; the `.txt` conversions can graduate with the spike.
- `spike-curl-probe/PREDICTIONS.md` — provenance artifact; graduates whole once the two citations re-point.

## Adjacent findings (contradictions / stale pointers)

- **`obs-routing-curl-spiral` contradicts itself about FE(8).** FE(8) (L25) states severity **is priced** (2026-07-24, council-accepted); Epistemic Status (L29) says "the pricing experiment (FE(8)) is still owed"; Working Notes L40 still says "Pricing experiment owed … before any severity claim." DECISIONS L1047 explicitly discharges the Working-Notes "owed." The segment gained FE(8) without the other two layers being updated — integration-is-replacement cleanup, two-line fix, worth doing in the same landing that deletes the breadcrumb.
- DECISIONS L501 "[⊘ unread]" on Coatléven 2020 — stale (item 2 above).
- Sweep-framing feedback: the brief's law "verify against the absorbing segment's *content*" needs one refinement for physics-measurement spikes — measured, council-accepted DECISIONS entries are functioning as claim-grade absorbing surface, with segments as summaries-plus-pointers. Other slices of the sweep whose spikes fed DECISIONS entries will look falsely unabsorbed if only `core/src/` counts.

## What is *not* here

No no-gos missing from core: the family's no-gos (Coatléven-on-MFD-8 makes it worse; naive-D4 tree not cube-safe; corner-was-never-a-conservation-bug; moment-tilt superseded; frozen stencil rejected) are all carried, mostly in DECISIONS with segment pointers. Nothing in these four dirs should stay live: every measurement has a live in-repo harness, so re-running beats re-mining.
