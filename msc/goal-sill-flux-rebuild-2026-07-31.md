# Goal: through rebuild + re-measure (sill → flux stack)

**Set:** 2026-07-31 night (Joseph: full autonomy through item 6; ignore SRC_HASH thrash; update tracking/probes/segments as landings close).

**Done when:** production path carries sill-graph + flux half; matched-ratio arms run; first-light (or equivalent) rebuilt under that stack; structural re-measure recorded; living surfaces present-tense.

## Flattened remaining

| # | Task | Status |
|---|---|---|
| 1 | Present-true stale segments (agent drift list) | **done** |
| 2 | Wire sill-graph into Jacobi + real memo/key | **done** (`sill1`; lake re-measure still open) |
| 3 | Flux-half record (per seam/stage, keyed) | **done** (`flux_record.rs`) |
| 4 | Flux-half in carve loop | **done** (Jacobi inflow inject; `flux1`) |
| 5 | Matched-ratio re-measure | **done** (`matched_ratio_probe`; empirical — not law) |
| 6 | Rebuild + re-measure under that stack | **in progress** |

## Law anchors (do not invert)

- Datum before flux (`#form-same-level-halo-exchange` FE(1)/(9); Bangerth/eSCAPE WN)
- No per-basin `SpillLevel` exchange object (constructively refuted)
- Matched-ratio may parallel wire for schedule science; must not replace sill/flux as world cut
- Structural stats only — no pointwise single-field agreement as success
- Integration is replacement on segment present-tense

## Session discipline

- Commit often with archaeologist messages
- Convict with tests/probes before claiming
- Hotspots + CONSOLIDATION + claim ES/WN present-true on each landing
- Peer-audit optional at wire and flux landings if residual feels load-bearing

## Log

- **2026-07-31 night:** Goal set. P4 done-for-now. `sill_graph` object+FE(9) tests already landed (`b4caf10`). Present-true pass starts.
- **2026-07-31 night:** Present-true committed. Jacobi sill inject: region extract → spill surface → `Fluvial::region_spill` between cadence chunks; halo key `sill1`. Next: flux half.
- **2026-07-31 night:** Flux half wire (`flux_record` + Jacobi inject, key `flux1`). Matched-ratio probe: L13 span128 tile64 epochs40 — within-ratio spreads 0.88 / 3.02 / 5.53 m at d/σ 1.0 / 0.8 / 1.6; **not** promoting ratio to law. Rebuild under sill1+flux1 started.
