---
slug: form-isostasy-column
type: formulation
status: robust-qualitative
stage: draft
depends:
  - form-column-control-volume
  - form-derived-sea-level
  - form-ordinum-governs-flux-web
  - obs-hydrosphere-box-nomos
  - disc-check-the-ladder
---

# Isostasy reads a lithosphere column; uplift rate is not a driver

Emerged land is earned by mass-conserving lithospheric freeboard — crust and depleted mantle keel as a conserved column, elevation as a derived reading — not by a strictly-positive height-rate field that only ever goes up.

## Formal Expression

1. **Primitive is column mass, not height rate.** The physical object is a **lithospheric column** per cell (or control volume): crustal thickness and density, plus keel (continental lithospheric mantle) thickness and depletion. **Elevation is derived** by isostatic balance over that column. Rock "uplift rate" is $\mathrm{d}e/\mathrm{d}t$ of that reading — a **diagnostic**, not a conserved driver. This is the same discipline as #form-column-control-volume (conserved material, not free height) applied to the solid Earth.

2. **Nomos chain (accepted shape).**  
   $$\mathrm{mantle\text{-}thermal} \rightarrow \mathrm{lithosphere} \rightarrow \mathrm{isostasy} \rightarrow \mathrm{sea\text{-}level} \rightarrow \mathrm{erosion}$$
   - **mantle-thermal** — secular cooling and melt fraction (the control parameter for long-term freeboard evolution).  
   - **lithosphere** — conserved columnar inventory (crust + keel), analogous in pattern to the hydrosphere box on rock ( #obs-hydrosphere-box-nomos ).  
   - **isostasy** — consumes the column, produces **surface elevation**; the compensation constant is fixed by **global mass balance**, so rise here is subsidence there without a second ad-hoc field.  
   - **sea-level** — ocean stock + solid hypsometry ( #form-derived-sea-level ).  
   - **erosion** — consumes elevation and **returns sediment mass** to the column (rock-mass ledger; isostatic rebound bounds relief). v1 built at the pour grain (FE(9)); the fine-grain fluvial-tile carving ( #detail-erosion-composition ) is a separate rung that does not yet debit the column.  
   Under this shape, a nomos named `uplift` that **produces a height rate** is the wrong article of law.

3. **Keel is half the freeboard, not optional.** Isostatic balance is over the **whole lithosphere**, compensated at the base of the continental lithospheric mantle — not crust alone. Melt that builds thick felsic crust leaves a Mg-rich, Fe-poor **depleted residue** (the buoyant keel). One mass-conserving process yields **two buoyant products stacked in one column**. Thickness typically dominates density contrast for crustal freeboard; the keel's small density contrast acts over great depth.

4. **Subsidence is the same physics with the driver off.** When magmatism ends and the column cools, edifices collapse and freeboard falls — re-submergence is not a bolted-on negative term. A field that is **strictly positive everywhere** cannot express basins, bimodal hypsometry, or "sit low."

5. **Plates are not required for first land.** Early cratonic emergence can be driven by magmatic differentiation + isostasy + cooling; compressional tectonics and orogeny add later relief. The ordinum ladder (water-world → transient volcanic emergence → stable freeboard → later high topography) is the same sequence the freeboard literature narrates ( #disc-check-the-ladder ).

6. **Predicate shape for `emerged-land` (literature-sourced acceptance tests, not modern-Earth bake-in).** Candid falsifiers are order-of-magnitude bands: land fraction $\gtrsim 10\%$ by ~3.0 Ga-equivalent (early-Earth span **~1.8–20%**, Flament floor to Korenaga ceiling); craton elevations ~1–1.5 km with peak relief ~1 km under a hot geotherm; $\gtrsim 2$ km topography unlikely while the crust is weak; modern-like freeboard only later (~2.5–2.2 Ga-equivalent). These are **Record-style checks** on earned freeboard, never licenses to bake land fraction into the Protogenic prior ( #form-derived-sea-level FE(7)). **Live predicate (2026-07-24).** `sea_level::emerged_land_verdict` adjudicates a Record per world-seed against these bands, split by what v1 can convict: **hard, time-free** — *land rises above sea* and *land fraction in band*; **soft** — *peak subaerial stand $\le 2$ km*, measured on the **pre-erosion** tectonic surface where the strength limit and erosion mass-return that would enforce it are open (FE(8)), so a breach is an amber **flag**, not a conviction; **not-yet-predicable** — the era-sharpening/timing clauses, since deep time does not run (so "per cycle" collapses to "per seed"). Probe: `examples/emerged_land_probe.rs`; unit tests convict the live world's hard clauses and fail both a floor known-bad (bathymetry-only water-world, 0%) and a ceiling known-bad (decreed sea, ~40%). This is the Claimed → Kept *instrument*, not a Kept verdict.

7. **Live state (chain head now runs in time, 2026-07-24).** `lithosphere.rs` carries the chain's convicting rungs: a per-cell **column** (craton felsic crust + depleted keel over thermally-ramped oceanic crust) and the **Airy isostasy read** — freeboard = buoyancy height − global mass-balance reference, zero-mean by construction on the reference grid (rise here *is* subsidence there, unit-tested exact), spanning basins and land (the range/reachability conviction, unit-tested). The fBm freeboard stand-in is **retired**; the pour reads the column ( #form-derived-sea-level ), and the ordinum's `promise[emerged-land]` is **Claimed** `:kept-by isostasy` — Claimed, not Kept ( #form-ordinum-governs-flux-web ). **The mantle-thermal driver is now a nomos, not a constant.** `mantle_thermal.rs` promotes `MANTLE_TP_C` to a declared secular-cooling **trajectory** $T_p(t)$ (physics **Low** — an order-of-magnitude curve through a present-Abyssal anchor toward the modern asymptote, monotone in time; **not** integrated from a heat budget, Korenaga 2017 still unread). Because the column and the pour are already pure in $T_p$, a memoized epoch chain re-derives freeboard and sea level along the cooling curve, so **emergence runs in time**: measured (`mantle_cooling_probe`, level 7, seeds 0/1/7) the land-fraction trajectory is **monotone non-decreasing** from a **near-water-world** at the hot early-Abyssal end (~0.0–0.1 % at $T_p\approx1641$) through the **in-band present** (4.6 / 2.1 / 4.9 % at $T_p=1550$ — equal to the live seed-only world, the coherence pin) into the cooling late-Abyssal (11–28 %). For two of three seeds the coldest epochs cross **above** the early-Earth ~20 % ceiling: honest present-truth — those epochs are the world maturing *past* the early-Earth regime toward modern-like freeboard (the "later era" clause the static predicate named *not-yet-predicable* now runs crudely), **not** a calibrated Abyssal fraction claim at every epoch. The `uplift` nomos survives as the rate-field *driver* erosion carves against — diagnostic-grade, no longer a freeboard keeper. Cooling-grows-contrast (the buoyancy half) is a unit-tested monotonicity; the land-fraction emergence is now its own conviction (`sea_level::cooling_grows_land_from_a_water_world_through_the_present`).

8. **Still open modelling (not this formulation).** Differentiation *rate law* (v1 columns are a fated stand-in — declared, physics Low); the mantle-thermal **rate law itself** (the cooling *nomos* now exists — FE(7) — but its trajectory is declared crude, physics Low, not calibrated: Korenaga et al. 2017 unread, no plate history); water loading in the balance (the $\rho_{sw}$ terms — declared v1 omission); full freeboard coefficients from primary sources not re-derived in-repo. Erosion's mass **return** to the column (rock-mass ledger; isostatic rebound bounding relief) is **v1 built** — see FE(9). Do not implement plates as a state variable to "get land." **The temporal predicate clauses** (era-sharpening, timing) named *not-yet-predicable* in FE(6) are now *approachable* — the cooling chain makes deep time run crudely — but wiring them into `emerged_land_verdict` as convictions is owed work, not done here.

9. **Rock-mass ledger — erosion returns mass to the column, rebound bounds relief (v1 built, 2026-07-24).** `erosion_return.rs` closes the chain's last-open rung at the pour grain. **Debit:** subaerial columns lose crust by a declared relief-driven law (`crust_eroded_m` $= \varphi\cdot\mathrm{stand}_+$ above sea, physics **Low** — a stand-in for $\int$ stream-power where the pour grain has no drainage, sibling to the fated inventory; clamped to the felsic pile, never the oceanic floor). **Rebound:** automatic in the Airy read — removing crust $\delta$ lowers the stand by only $\delta(\rho_a-\rho_c)/\rho_a\approx 17\%$ for felsic crust, the other ~83 % rebounds (`surface_drop_per_crust_removed`, unit-tested). This is why the ledger *bounds* relief toward the band rather than planing it flat. **Credit:** the eroded mass is **returned to the column** as a uniform submarine sediment blanket ($\rho_{sed}$ = 2500), so basins fill and load-subside while cratons erode-and-rebound — relief compresses from both ends. **The conservation claim (failable):** $\sum(\mathrm{crust}\cdot\rho_c + \mathrm{sed}\cdot\rho_{sed})\cdot\mathrm{area}$ is invariant across the pass to float precision (`rock_mass_is_conserved_across_the_ledger_pass`, the closed-box probe); the post-erosion freeboard re-references to zero mean (rise here IS subsidence there, still — unit-tested). **Calibration observation (measured, level 8, $\varphi=1$):** the amber over-stand trims toward the band — seed 0 **2436 → 2025 m**, seed 7 **2386 → 1982 m** (amber → in-band), seed 1 1724 → 1435 m; the ~17 % trim equals the felsic rebound fraction (`examples/rock_mass_ledger_probe.rs`). Purity is kept: the per-cell debit/credit read memoized global integrals, so the post-erosion column is a pure function of $(seed, cell, T_p)$ — a single erosion *maturity* step, like `mantle_thermal`'s discrete epochs. **This is an OBSERVATION** (DECISIONS `proposed`), **not a Kept mark, and it does not change the default surface.** **Named, not done here:** wiring the post-erosion surface into the *default* pour (globe-visible; an adoption step bordering the view); routed/proximity-weighted deposition (v1 is a uniform blanket); the iterated erode→rebound→re-erode epoch chain (v1 is one step); the decl upgrade `LITHO_COLUMN` `NotTracked`→`Conserved` once the ledger is the live default.

## Epistemic Status

**Max attainable:** **exact** for the architectural claim that elevation must be a mass-conserving reading of a lithosphere column if Abyssal freeboard is to be keepable; **empirical / literature** for the Chowdhury–Cawood–Mulder 2025 mechanism narrative and numeric predicate bands; **heuristic** for any particular rate law until modelled and probed.

**Currently `robust-qualitative`:** nomos shape and "uplift-as-rate-producer is the wrong article for freeboard" are **council-accepted** (`DECISIONS[isostasy-is-the-uplift-nomos-and-the-keel-is-half-of-it]`, `:council 2026-07-24`). **v1 implementation** (column + Airy read + pour wiring + Claimed keeper + the mantle-thermal cooling nomos) is **code- and test-verified** (`lithosphere.rs` / `mantle_thermal.rs` convictions; ordinum Claimed test; monotone emergence from a water-world through the in-band present). The build ledger entries for these rungs are **proposed** (design accepted; build verdicts fresh — not yet council-sealed as engineering; the mantle-thermal nomos was *scope*-fronted at council, `DECISIONS[mantle-thermal-nomos-ruled-in-scope]`, but its build is unsealed). The mantle-thermal **rate law** (crude, physics Low), differentiation rate law, and water loading remain **open** (FE(8)); the **rock-mass ledger is v1 built** (FE(9)) — a measured operator with a closed-box conservation probe and the amber-trim observation, not yet the default surface. Architecture accepted at council; literature citations ride the decision's primary-read chain.

Stage `draft`. **Implementation claim (bounded):** `lithosphere` and `isostasy` are live and registered with the v1 scope FE(7) states — inventory stand-in (physics Low), balance read (physics Med), freeboard may be negative by construction. **Not claimed:** Kept; anything in FE(8); plate emergence.

## Discussion

Sea level already pours a conserved ocean against solid hypsometry. Without a mass-conserving solid column on the other side of that pour, "emerged land" is either baked into a prior (forbidden by the ladder) or faked by a one-sided rate field. The isostasy column is the missing dual of the hydrosphere box: rock inventory with a readable freeboard, so erosion and sea-level both consume truth rather than costume.

## Working Notes

- **Live modules:** `mantle_thermal.rs` (cooling trajectory $T_p(t)$, the chain head); `lithosphere.rs` (column with `sediment_m` + `freeboard_m` + `freeboard_m_at_tp` + `surface_drop_per_crust_removed` rebound identity; `reference_m_at_tp` memoized per `(seed,T_p)` — the O(N²) trap the cooling pour would otherwise fall into); `sea_level.rs` pour (seed-only + `derived_sea_level_at`/`_at_tp` epoch chain; `pour_ocean_over` factored so the ledger re-pours with the identical inversion); `erosion_return.rs` (FE(9) rock-mass ledger — pure post-erosion column/freeboard/sea/record + closed-box balance); `uplift.rs` **rate only**; nomotheke `MANTLE_THERMAL` → `LITHOSPHERE` (consumes `mantle potential temperature`) → `ISOSTASY`; erosion consumes `ROCK_UPLIFT_RATE` and emerged-land geometry from isostasy.
- **Cooling does not conserve column mass across epochs (declared).** The thermal driver rewrites oceanic-crust thickness/density as a *declared* effect with no melt-flux accounting — the differentiation/rock-mass ledger that would balance it is open. The *within-epoch* balance still holds exactly (the reference is re-integrated per $T_p$, area-mean freeboard ≡ 0). Neither mints global elevation.
- **View:** `examples/emerging_land.rs` — the cooling chain as a from-space ASCII globe sequence (land rising as basins deepen); `examples/mantle_cooling_probe.rs` — the measured emergence trajectory + invariants.
- **Do not re-open:** fBm freeboard as land keeper; baking bimodal land into the bathymetry prior; plate state-fields as the path to first freeboard; claiming Kept from Claimed alone.
- **Regression guards (tests):** range spans zero; mean freeboard ≡ 0 at any $T_p$; cooling grows craton/ocean contrast; keel share > 15%; craton-fraction calibration band; ordinum Claimed-by-isostasy (history of unkept-is-the-point in the test comment).
- **Record predicate built (2026-07-24):** `sea_level::emerged_land_verdict` + `examples/emerged_land_probe.rs` (FE(6)). Measured, level 8, seeds 0/1/7: land fraction **4.5 / 2.1 / 4.8 %** (in-band, near the Flament floor); peak subaerial stand **2436 / 1724 / 2386 m** — seeds 0 and 7 **amber-flag** the ~2 km relief ceiling, an honest signal that isostatic freeboard *over-stands* the craton on the pre-erosion surface (erosion + strength-limit trim is open, FE(8)). Hard clauses green; **still Claimed, not Kept.**
- **Rock-mass ledger built (2026-07-24):** `erosion_return.rs` (FE(9)) — debit (relief-driven crust erosion) + rebound (automatic Airy) + credit (submarine sediment blanket), closed-box mass conserved to float precision. Measured amber trim (level 8, φ=1): seed 0 2436→2025 m, seed 7 2386→1982 m (into band), seed 1 1724→1435 m. Tests: `erosion_return::{rock_mass_is_conserved…, post_erosion_freeboard_is_zero_mean…, ledger_trims_the_amber…, deterministic}`; `lithosphere::erosional_unloading_rebounds…`; probe `examples/rock_mass_ledger_probe.rs`. Default surface untouched (measured operator, DECISIONS `proposed`).
- **Next convicting work:** wire the post-erosion surface into the *default* pour (globe-visible adoption step, borders view); routed/proximity-weighted deposition (v1 uniform blanket); iterated erode→rebound epochs (v1 one maturity step); water loading; generic BrokenKeeper range/reachability machinery; wiring the now-approachable temporal (per-cycle / Ga-equivalent) predicate clauses into `emerged_land_verdict` — deep time now runs crudely along the cooling chain (`mantle_thermal::abyssal_epochs`).
- **Primary literature:** Chowdhury, Cawood & Mulder 2025 (relata: chowdhury-2025-subaerial); early-continents survey for Record targets.
- Sibling: #detail-erosion-composition ; #detail-phase-abyssal ; open mantle-thermal; router under #obs-cube-locked-kernel-bias .
