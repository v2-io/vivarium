---
slug: form-isostasy-column
type: formulation
status: robust-qualitative
stage: draft
depends:
  - form-column-control-volume
  - form-derived-sea-level
  - form-ordinum-governs-flux-web
  - obs-hydrosphere-box-nomos
  - disc-check-the-ladder
---

# Isostasy reads a lithosphere column; uplift rate is not a driver

Emerged land is earned by mass-conserving lithospheric freeboard — crust and depleted mantle keel as a conserved column, elevation as a derived reading — not by a strictly-positive height-rate field that only ever goes up.

## Formal Expression

1. **Primitive is column mass, not height rate.** The physical object is a **lithospheric column** per cell (or control volume): crustal thickness and density, plus keel (continental lithospheric mantle) thickness and depletion. **Elevation is derived** by isostatic balance over that column. Rock "uplift rate" is $\mathrm{d}e/\mathrm{d}t$ of that reading — a **diagnostic**, not a conserved driver. This is the same discipline as #form-column-control-volume (conserved material, not free height) applied to the solid Earth.

2. **Nomos chain (accepted shape).**  
   $$\mathrm{mantle\text{-}thermal} \rightarrow \mathrm{lithosphere} \rightarrow \mathrm{isostasy} \rightarrow \mathrm{sea\text{-}level} \rightarrow \mathrm{erosion}$$
   - **mantle-thermal** — secular cooling and melt fraction (the control parameter for long-term freeboard evolution).  
   - **lithosphere** — conserved columnar inventory (crust + keel), analogous in pattern to the hydrosphere box on rock ( #obs-hydrosphere-box-nomos ).  
   - **isostasy** — consumes the column, produces **surface elevation**; the compensation constant is fixed by **global mass balance**, so rise here is subsidence there without a second ad-hoc field.  
   - **sea-level** — ocean stock + solid hypsometry ( #form-derived-sea-level ).  
   - **erosion** — consumes elevation and **returns sediment mass** to the column (rock-mass ledger; isostatic rebound bounds relief). v1 built at the pour grain (FE(9)); the fine-grain fluvial-tile carving ( #detail-erosion-composition ) is a separate rung that does not yet debit the column.  
   Under this shape, a nomos named `uplift` that **produces a height rate** is the wrong article of law.

3. **Keel is half the freeboard, not optional.** Isostatic balance is over the **whole lithosphere**, compensated at the base of the continental lithospheric mantle — not crust alone. Melt that builds thick felsic crust leaves a Mg-rich, Fe-poor **depleted residue** (the buoyant keel). One mass-conserving process yields **two buoyant products stacked in one column**. Thickness typically dominates density contrast for crustal freeboard; the keel's small density contrast acts over great depth.

4. **Subsidence is the same physics with the driver off.** When magmatism ends and the column cools, edifices collapse and freeboard falls — re-submergence is not a bolted-on negative term. A field that is **strictly positive everywhere** cannot express basins, bimodal hypsometry, or "sit low."

5. **Plates are not required for first land.** Early cratonic emergence can be driven by magmatic differentiation + isostasy + cooling; compressional tectonics and orogeny add later relief. The ordinum ladder (water-world → transient volcanic emergence → stable freeboard → later high topography) is the same sequence the freeboard literature narrates ( #disc-check-the-ladder ).

6. **Predicate shape for `emerged-land` (literature-sourced acceptance tests, not modern-Earth bake-in).** Candid falsifiers are order-of-magnitude bands: land fraction $\gtrsim 10\%$ by ~3.0 Ga-equivalent (early-Earth span **~1.8–20%**, Flament floor to Korenaga ceiling); craton elevations ~1–1.5 km with peak relief ~1 km under a hot geotherm; $\gtrsim 2$ km topography unlikely while the crust is weak; modern-like freeboard only later (~2.5–2.2 Ga-equivalent). These are **Record-style checks** on earned freeboard, never licenses to bake land fraction into the Protogenic prior ( #form-derived-sea-level FE(7)). **Live predicate (2026-07-24).** `sea_level::emerged_land_verdict` adjudicates a Record per world-seed against these bands, split by what v1 can convict: **hard, time-free** — *land rises above sea* and *land fraction in band*; **soft** — *peak subaerial stand $\le 2$ km*, measured on the **live (post-rock-mass-ledger) tectonic surface** since the FE(9) adoption; the ledger's erosion mass-return + rebound now trims the pre-erosion over-stand toward the band by ~17 % (the felsic rebound fraction) — seeds 0/1/7 post-ledger ~2192 / 2033 / 2274 m, all a soft amber **flag** (coherent nucleation-growth cratons concentrate the felsic pile, so they stand higher than the retired dispersed speckle and all three residual-breach), signalling v1 ledger crudeness ($\varphi=1$, uniform blanket) + the still-open strength limit, not a conviction; **temporal** — split by what the crude rate law can earn: the *calibration-free* era clauses (**monotone emergence**, **submerged start**, **in-band entry-and-stay**) now **convict** along the cooling chain (`emerged_land_timing_verdict`, FE(7)) — deep time runs crudely since the mantle-thermal trajectory is a real time axis — while the literature's *Ga-absolute* clauses ("≳10 % by ~3.0 Ga"; "modern freeboard only ~2.5 Ga") stay **not-yet-predicable** because the epoch→Ga mapping is declared, not calibrated. Probes: `examples/emerged_land_probe.rs` (static Record) and `examples/mantle_cooling_probe.rs` (the temporal trajectory + verdict); unit tests convict the live world's hard clauses and fail both a floor known-bad (bathymetry-only water-world, 0%) and a ceiling known-bad (decreed sea, ~40%), and the timing verdict fails four synthetic temporal known-bads. This is the Claimed → Kept *instrument*, not a Kept verdict.

7. **Live state (chain head now runs in time, 2026-07-24).** `lithosphere.rs` carries the chain's convicting rungs: a per-cell **column** (craton felsic crust + depleted keel over thermally-ramped oceanic crust) — where the craton **placement** is a fated **nucleation-and-growth** field (a few blue-noise nuclei accreting coherent cratons of a characteristic scale, *not* a threshold slice through scale-free fBm, whose percolation-speckle morphology is the wrong class however honest the heights; the cratons **accrete outward as the mantle cools**, so emergence reads as continents assembling — FE(8), `DECISIONS[craton-nucleation-and-deep-time-playback-ruled-in-scope]`) — and the **Airy isostasy read** — freeboard = buoyancy height − global mass-balance reference, zero-mean by construction on the reference grid (rise here *is* subsidence there, unit-tested exact), spanning basins and land (the range/reachability conviction, unit-tested). The fBm freeboard stand-in is **retired**; the pour reads the column ( #form-derived-sea-level ), and the ordinum's `promise[emerged-land]` is **Claimed** `:kept-by isostasy` — Claimed, not Kept ( #form-ordinum-governs-flux-web ). **The mantle-thermal driver is now a nomos, not a constant.** `mantle_thermal.rs` promotes `MANTLE_TP_C` to a declared secular-cooling **trajectory** $T_p(t)$ (physics **Low** — an order-of-magnitude curve through a present-Abyssal anchor toward the modern asymptote, monotone in time; **not** integrated from a heat budget, Korenaga 2017 still unread). Because the column and the pour are already pure in $T_p$, a memoized epoch chain re-derives freeboard and sea level along the cooling curve, so **emergence runs in time**: measured (`mantle_cooling_probe`, level 7, seeds 0/1/7) the land-fraction trajectory is **monotone non-decreasing** from a **near-water-world** at the hot early-Abyssal end (~0.0–0.9 % at $T_p\approx1641$) through the **in-band present** (4.7 / 3.7 / 5.1 % at $T_p=1550$ — equal to the live seed-only world, the coherence pin) into the cooling late-Abyssal (14–19 %). Under nucleation-growth the emergence has **two** monotone drivers: basins deepen (oceanic crust cooling) *and* cratons accrete outward (growth ∝ cooling, saturating so the reference-rise never re-submerges land — the monotonicity is preserved by construction, not luck). **The whole modeled Abyssal span stays *in-band*:** the coldest epoch (2.6 Ga-equivalent) measures 14.5 / 15.6 / 18.7 %, under the ~20 % early-Earth ceiling — nucleation's *saturating* craton growth bounds the cold end, so emergence reads as a clean drowned → in-band trajectory that enters the band at the present anchor and does not overshoot it within the modeled window. (The retired dispersed-fBm placement overshot to ~27 % at the cold end and would fail this — the finding *moved* when nucleation-growth landed; recorded whichever way it went, per the probe.) **Temporal clauses now convict (crudely).** Because the trajectory is a real time axis, `sea_level::emerged_land_timing_verdict` convicts the *calibration-free* era clauses that were `NotPredicable` when "per cycle" collapsed to per-seed — **monotone emergence**, a **submerged (water-world) start**, and **in-band entry-and-stay** — for all three seeds, with synthetic known-bads (a non-monotone dip; a >20 % overshoot; a never-emerging drowned world; a hot-start-already-emerged world) each failing the matching clause. The literature's **Ga-absolute** clauses ("land ≳10 % by ~3.0 Ga-equivalent"; "modern-like freeboard only ~2.5–2.2 Ga") stay `NotPredicable`: the epoch→Ga mapping is a declared order-of-magnitude curve (`PRESENT_ABYSSAL_GA`, τ), so an absolute-Ga target is a calibration the crude rate law cannot earn — the curve *passes near* the 10 %-by-3.0-Ga target (10.8 / 11.4 / 13.3 % at the 3.0-Ga-labeled epoch), an encouraging coincidence **recorded, not convicted**. The `uplift` nomos survives as the rate-field *driver* erosion carves against — diagnostic-grade, no longer a freeboard keeper. Cooling-grows-contrast (the buoyancy half) is a unit-tested monotonicity; the land-fraction emergence is now its own conviction (`sea_level::cooling_grows_land_from_a_water_world_through_the_present`), and its temporal shape is `sea_level::emerged_land_timing_verdict`.

8. **Still open modelling (not this formulation).** Differentiation *rate law* — the **horizontal half** (craton placement) now has a v1 **nucleation-and-growth** stand-in (few fated nuclei, coherent accretion of characteristic scale, growth composing with the cooling trajectory; physics Low-Med, declared — sites and growth are fated *geometry*, not differentiation *dynamics*, which stays open) built + convicted 2026-07-24 (`craton_fraction_is_in_band` for present area; `craton_morphology` for the shape class — few coherent cratons vs the retired fBm's percolation speckle, at a matched land budget); the **vertical half** (heights, mass, rebound) is FE(7)/FE(9). Still fated (no melt/plate dynamics): the mantle-thermal **rate law itself** (the cooling *nomos* now exists — FE(7) — but its trajectory is declared crude, physics Low, not calibrated: Korenaga et al. 2017 unread, no plate history); water loading in the balance (the $\rho_{sw}$ terms — declared v1 omission); full freeboard coefficients from primary sources not re-derived in-repo. Erosion's mass **return** to the column (rock-mass ledger; isostatic rebound bounding relief) is **v1 built** — see FE(9). Do not implement plates as a state variable to "get land." **The temporal predicate clauses** are now *split*: the calibration-free ones (monotone emergence, submerged start, in-band entry-and-stay) are **convicted** (`emerged_land_timing_verdict`, FE(6)/(7)); the literature's **Ga-absolute** clauses ("≳10 % by ~3.0 Ga"; "modern-like freeboard only ~2.5–2.2 Ga") stay **open** — they need a *calibrated* rate law (the mantle-thermal trajectory is declared crude), so an absolute-Ga fraction/era target is unearned in v1.

9. **Rock-mass ledger — erosion returns mass to the column, rebound bounds relief (v1 built, 2026-07-24).** `erosion_return.rs` closes the chain's last-open rung at the pour grain. **Debit:** subaerial columns lose crust by a declared relief-driven law (`crust_eroded_m` $= \varphi\cdot\mathrm{stand}_+$ above sea, physics **Low** — a stand-in for $\int$ stream-power where the pour grain has no drainage, sibling to the fated inventory; clamped to the felsic pile, never the oceanic floor). **Rebound:** automatic in the Airy read — removing crust $\delta$ lowers the stand by only $\delta(\rho_a-\rho_c)/\rho_a\approx 17\%$ for felsic crust, the other ~83 % rebounds (`surface_drop_per_crust_removed`, unit-tested). This is why the ledger *bounds* relief toward the band rather than planing it flat. **Credit:** the eroded mass is **returned to the column** as a uniform submarine sediment blanket ($\rho_{sed}$ = 2500), so basins fill and load-subside while cratons erode-and-rebound — relief compresses from both ends. **The conservation claim (failable):** $\sum(\mathrm{crust}\cdot\rho_c + \mathrm{sed}\cdot\rho_{sed})\cdot\mathrm{area}$ is invariant across the pass to float precision (`rock_mass_is_conserved_across_the_ledger_pass`, the closed-box probe); the post-erosion freeboard re-references to zero mean (rise here IS subsidence there, still — unit-tested). **Calibration observation (measured, level 8, $\varphi=1$, nucleation-growth field):** the amber over-stand trims toward the band by the felsic rebound fraction — seed 0 **2636 → 2192 m**, seed 1 **2444 → 2033 m**, seed 7 **2738 → 2274 m** (~17 % trim, all residual-amber: coherent cratons stand higher than the retired speckle) (`examples/rock_mass_ledger_probe.rs`). Purity is kept: the per-cell debit/credit read memoized global integrals, so the post-erosion column is a pure function of $(seed, cell, T_p)$ — a single erosion *maturity* step, like `mantle_thermal`'s discrete epochs. **ADOPTED as the live default surface (2026-07-24, council-ruled under Joseph's standing grant).** `sea_level::{tectonic_surface_at_tp, derived_sea_level_at_tp}` — and thus every downstream reader (the pour, land classification, `emerged_land_record`, the globe, `gen`, `query`, erosion outlets) — now read *this* post-erosion surface; the raw isostatic surface is retained as `sea_level::*_pre_ledger_*` (the ledger consumes it; probes compare against it). Correspondingly `LITHO_COLUMN`'s promise is now `Conservation::Conserved` with the closed-box probe as its convicting instrument. Rationale (council): the ledger is the *honest* surface — the pre-ledger one over-stands by known-missing physics — and `#norm-declared-violation-is-not-license` cuts against leaving a built conservation fix unwired as a display piece. **Still Claimed, not Kept.** **Open rungs (named, not this adoption):** routed/proximity-weighted deposition (v1 is a uniform submarine blanket); the iterated erode→rebound→re-erode epoch chain (v1 is one $\varphi=1$ step); water loading; wiring the ledger as its own flux-web nomos edge (a nicety — #form-flux-web).

## Epistemic Status

**Max attainable:** **exact** for the architectural claim that elevation must be a mass-conserving reading of a lithosphere column if Abyssal freeboard is to be keepable; **empirical / literature** for the Chowdhury–Cawood–Mulder 2025 mechanism narrative and numeric predicate bands; **heuristic** for any particular rate law until modelled and probed.

**Currently `robust-qualitative`:** nomos shape and "uplift-as-rate-producer is the wrong article for freeboard" are **council-accepted** (`DECISIONS[isostasy-is-the-uplift-nomos-and-the-keel-is-half-of-it]`, `:council 2026-07-24`). **v1 implementation** (column + Airy read + pour wiring + Claimed keeper + the mantle-thermal cooling nomos) is **code- and test-verified** (`lithosphere.rs` / `mantle_thermal.rs` convictions; ordinum Claimed test; monotone emergence from a water-world through the in-band present). The build ledger entries for these rungs are **proposed** (design accepted; build verdicts fresh — not yet council-sealed as engineering; the mantle-thermal nomos was *scope*-fronted at council, `DECISIONS[mantle-thermal-nomos-ruled-in-scope]`, but its build is unsealed). The mantle-thermal **rate law** (crude, physics Low) and water loading remain **open** (FE(8)); the differentiation rate law's **horizontal half** (craton placement) is v1 **nucleation-growth** built + convicted (present area + morphology + monotone emergence), its **dynamics** still open; the **rock-mass ledger is v1 built and ADOPTED as the live default surface** (FE(9), council-ruled 2026-07-24) — a closed-box conservation probe convicts it and `LITHO_COLUMN` is now `Conserved`. Architecture accepted at council; literature citations ride the decision's primary-read chain.

Stage `draft`. **Implementation claim (bounded):** `lithosphere` and `isostasy` are live and registered with the v1 scope FE(7) states — inventory stand-in (physics Low), balance read (physics Med), freeboard may be negative by construction. **Not claimed:** Kept; anything in FE(8); plate emergence.

## Discussion

Sea level already pours a conserved ocean against solid hypsometry. Without a mass-conserving solid column on the other side of that pour, "emerged land" is either baked into a prior (forbidden by the ladder) or faked by a one-sided rate field. The isostasy column is the missing dual of the hydrosphere box: rock inventory with a readable freeboard, so erosion and sea-level both consume truth rather than costume.

## Working Notes

- **Live modules:** `mantle_thermal.rs` (cooling trajectory $T_p(t)$, the chain head); `lithosphere.rs` (column with `sediment_m` + `freeboard_m` + `freeboard_m_at_tp` + `surface_drop_per_crust_removed` rebound identity; craton **nucleation-growth** placement `craton_sites`/`craton_growth`/`craton_weight_at_tp` memoized sites per seed; `reference_m_at_tp` memoized per `(seed,T_p)` — the O(N²) trap the cooling pour would otherwise fall into); `craton_morphology.rs` (the shape-class probe, matched-budget fragmentation); `sea_level.rs` pour (seed-only + `derived_sea_level_at`/`_at_tp` epoch chain; `pour_ocean_over` factored so the ledger re-pours with the identical inversion); `erosion_return.rs` (FE(9) rock-mass ledger — pure post-erosion column/freeboard/sea/record + closed-box balance); `uplift.rs` **rate only**; nomotheke `MANTLE_THERMAL` → `LITHOSPHERE` (consumes `mantle potential temperature`) → `ISOSTASY`; erosion consumes `ROCK_UPLIFT_RATE` and emerged-land geometry from isostasy.
- **Cooling does not conserve column mass across epochs (declared).** The thermal driver rewrites oceanic-crust thickness/density as a *declared* effect with no melt-flux accounting — the *differentiation* ledger that would balance the thermal rewrite is open. (The erosion rock-mass ledger, FE(9), conserves the *erosion* transport within a pass, but does not balance the thermal inventory rewrite — a distinct open ledger.) The *within-epoch* balance still holds exactly (the reference is re-integrated per $T_p$, area-mean freeboard ≡ 0). Neither mints global elevation.
- **View:** `examples/emerging_land.rs` — the cooling chain as a from-space ASCII globe sequence (land rising as basins deepen); `examples/mantle_cooling_probe.rs` — the measured emergence trajectory + invariants.
- **Do not re-open:** fBm freeboard as land keeper; **fBm-threshold craton placement (percolation speckle) as the differentiation stand-in** — retired for nucleation-growth, kept only as the morphology known-bad; baking bimodal land into the bathymetry prior; plate state-fields as the path to first freeboard; claiming Kept from Claimed alone.
- **Regression guards (tests):** range spans zero; mean freeboard ≡ 0 at any $T_p$; cooling grows craton/ocean contrast; keel share > 15%; craton-fraction calibration band; **craton morphology (few coherent cratons vs matched-budget fBm speckle)**; **monotone emergence along the cooling chain**; ordinum Claimed-by-isostasy (history of unkept-is-the-point in the test comment).
- **Craton nucleation-growth built (2026-07-24):** `lithosphere::{craton_sites, craton_growth, craton_weight_at_tp}` replaces the retired fBm-threshold placement (`craton_weight_fbm_speckle_known_bad`, kept as the morphology known-bad). Few blue-noise nuclei (8–13) accrete coherent cratons of characteristic scale (mean radius 0.235 rad) that GROW as the mantle cools (saturating so monotonicity holds). Convictions: `craton_fraction_is_in_band` (present area ~0.093, in band), `craton_morphology::{nucleation_growth_is_a_few_coherent_cratons, the_fbm_threshold_known_bad_fragments_at_matched_area}` (few coherent components vs ~2.3–2.8× fragmentation for scale-free fBm at a matched land budget), and the unchanged `cooling_grows_land…` monotone emergence. Probe: `examples/craton_morphology_probe.rs`. Consumers unchanged (same column blend, differently placed); differentiation *dynamics* remain open (FE(8)).
- **Record predicate built (2026-07-24):** `sea_level::emerged_land_verdict` + `examples/emerged_land_probe.rs` (FE(6)). The live verdict reads the **post-ledger** default surface on the nucleation-growth field: land fraction **4.68 / 3.68 / 5.08 %** (seeds 0/1/7, in-band), peak subaerial stand **2192 / 2033 / 2274 m** — all three a residual amber (coherent cratons stand higher than the retired speckle; v1 ledger crudeness + open strength limit). The *pre-ledger* isostatic surface (the ledger's input) stood at 2636 / 2444 / 2738 m. Hard clauses green; **still Claimed, not Kept.**
- **Rock-mass ledger built + adopted (2026-07-24):** `erosion_return.rs` (FE(9)) — debit (relief-driven crust erosion) + rebound (automatic Airy) + credit (submarine sediment blanket), closed-box mass conserved to float precision. It IS the live default surface (`sea_level` repoints; `LITHO_COLUMN` `Conserved`). Measured trim (level 8, φ=1, nucleation-growth): seed 0 2636→2192 m, seed 1 2444→2033 m, seed 7 2738→2274 m; ~17 % trim = felsic rebound fraction (all residual-amber). Tests: `erosion_return::{rock_mass_is_conserved…, post_erosion_freeboard_is_zero_mean…, ledger_trims_the_amber…, deterministic}`; `lithosphere::erosional_unloading_rebounds…`; probe `examples/rock_mass_ledger_probe.rs`.
- **Next convicting work:** routed/proximity-weighted deposition (v1 uniform blanket); iterated erode→rebound epochs (v1 one maturity step); water loading; store-as-save the derived (post-ledger) sea at `vivarium build` time (the pour is now ~4× heavier — pre-pour + ledger + post-pour — so the globe first-light provisional/refine path and a build-time memo matter more); generic BrokenKeeper range/reachability machinery. **The calibration-free temporal clauses are now convicted** (`emerged_land_timing_verdict`); what remains is a *calibrated* mantle-thermal rate law to earn the Ga-absolute era clauses — the one blocker between the crude curve and a real "≳10 % by 3.0 Ga" conviction.
- **Primary literature:** Chowdhury, Cawood & Mulder 2025 (relata: chowdhury-2025-subaerial); early-continents survey for Record targets.
- Sibling: #detail-erosion-composition ; #detail-phase-abyssal ; open mantle-thermal; router under #obs-cube-locked-kernel-bias .
