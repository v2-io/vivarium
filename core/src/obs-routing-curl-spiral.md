---
slug: obs-routing-curl-spiral
type: observation
status: exact
stage: draft
depends:
  - obs-cube-locked-kernel-bias
  - norm-probe-sensitivity
  - norm-bias-vs-noise
---

# Routing violates the contour-orthogonality identity (the flow spirals)

Measured: ~2% of routed flux runs *around* the hill instead of down it — a level-independent, topological, signed bias. The identity it violates had to be restated before it could be tested, and the obvious probe location is a null test that would have acquitted the router.

## Formal Expression

1. **The identity (restated — the naive form is false).** "Gravity-driven flow is curl-free" is **wrong**: real discharge is $q = -K\nabla\phi$ with varying conveyance $K$, so $\nabla \times q = -\nabla K \times \nabla\phi \neq 0$ — real water on a real hillslope *has* curl. The exact, pointwise, mesh-independent statement is: **flow direction is orthogonal to the contours of $\phi$**, i.e. $$\kappa(C) = \oint \hat d \cdot d\ell \Big/ \oint d\ell = \langle \sin\Delta\rangle = 0$$ on every closed contour. $\kappa$ is the fraction of transport running *along* the contour instead of down the slope; $\kappa \neq 0$ means the routed field spirals — it is not the gradient flow of the potential it claims (an Escher staircase).
2. **Measured violation.** $\kappa \approx 2.0\times 10^{-2}$ on the cone; median $1.9\times 10^{-2}$ over traced contours on real terrain. **Level-independent** (identical L9→L23, a $16\,384\times$ refinement) while the probe's own error falls five orders to machine zero. The spurious vorticity is a smooth, coherent, **signed** field — null island at the face centre, CCW over one half of the face, CW over the other, strengthening outward. Bias, topological; refinement never touches it ( #norm-bias-vs-noise ).
3. **The natural probe location is a null test.** A cone at a face centre is D4-symmetric: mirror symmetry forces $\oint \sin\Delta\, d\ell = 0$ **exactly**. Measured $\kappa = -1.9\times10^{-10}$ at the face centre while a $5.9°$ pointwise bias sits right there. The measurement exists only off-symmetry — where real terrain is worst. A face-centred probe *acquits* a defective router ( #norm-probe-sensitivity ).
4. **The proposed replacement does not fix it.** Gradient-projected edge flux: $\kappa = 4.5\times10^{-3}$ — $4.5\times$ better, **not** zero. The circulation is manufactured by the *split* of mass among discrete receivers, not by the direction estimate alone.
5. **What $\kappa$ actually measured (bound on FE(1)–(4)).** The probe read the tangential component of the **fan-weighted transport direction** — a scalar accumulation's implied direction, which is **not** the physical flow direction. The router's output is a boundary integral, ill-posed *in the continuum* (its value depends on cell shape and orientation — Coatléven & Chauveau, read primary); the physical direction is the **reconstructed flux vector** $\hat Q_K = Q_K/\lVert Q_K\rVert$. A first-moment tilt on the fan weights drove $\kappa \to 5\times10^{-6}$ but broke magnitude — because it forced *the wrong object* to have the right first moment. The apparent two-fixes-interfere paradox is dissolved: it was one fix stated in the wrong coordinates (`DECISIONS[the-router-is-a-scalar-pretending-to-be-a-vector-and-p-is-the-bias]` ④, `DECISIONS[mfds-output-is-not-a-discharge-it-is-a-boundary-integral]`).
6. **Remedy stack (composes in order).** (a) **Done 2026-07-24:** live MFD uses $p=1$ (weights $\propto \cos^q$ have exactly zero first moment at $q=1$ on a square lattice — a theorem; the old $0.24°$ "intrinsic" lattice baseline *was* the $p=1.1$ exponent). (b) **Still open:** kill the diagonals (they cross no face — 47.8% phantom flux — and make MFD un-correctable as an FV scheme); (c) **priced 2026-07-29** (`DECISIONS[fe6c-reconstruction-is-cube-safe-but-the-diagonal-kill-alone-is-cheaper]`; `examples/router_fe6c`, 8 seeds, null-pair-controlled): the Coatléven vector reconstruction is built and measured on the receiver tree — **~3× more cube-safe than the naive-D4 strawman** (RMS mean-CUBE 0.055 vs 0.177; the FE(8) belief, confirmed) — **and not more cube-safe than the cheapest arm**: diagonals killed, faces weighted by transmissivity, live D8 tree kept, at 0.033, closest to the metric's floor. *Channel orientation is therefore no longer an argument for the reconstruction*; its remaining justification is FE(5)'s consistency claim ($\tilde q_K$ has no mesh-independent limit), which this metric does not measure. Cost 1.06× wall-time; the reconstruction needs **halo 2** (neighbours' outgoing splits) independently of (d) — and **(d)'s marginal on cube-safety is within noise** (0.055 vs 0.057) while changing the drainage field substantially, so the (d)-before-(c) precondition stands on Coatléven's hypothesis structure, not on a measured cube-safety gain. **The magnitude half is unpriced:** consuming $\lVert Q_K\rVert$ means restating stream power in specific-catchment-area form with re-tuned erodibility — a real next spike, not a result. **Standing limit, narrowed by measurement:** the $s_K = 0$ carve-out is implemented and **inert** on an uplifting dome with base-level sinks (fill-raised cells 0.0–0.2%; moves CUBE < 0.003) — it bites on *endorheic* configurations, which remain untested. The superseded moment-tilt is retained only as the instrument that located the disease. Curl/spiral measurements above were taken under the old fan stack — re-probe after (b)–(c).
7. **Circulation and plume drift are different defects.** They do not rank routers the same way (edge flux: ~$900\times$ the circulation of moment-corrected MFD, yet less than half the drift) — convergent vs solenoidal parts (Helmholtz split). The fan probe ( #obs-cube-locked-kernel-bias ) and the curl probe do not subsume each other. Under refinement, every fan-based variant's catchment error **grows**; only the real-face FV router is flat.
8. **Severity is priced (2026-07-24), and it reorders the remedy stack.** The pricing experiment ran (`examples/router_pricing`, against the pre-lakes kernel — its `LiveMfd` arm no longer bit-matches `erosion.rs` since the fill-restore work; the result **replicates** on the current kernel over 8 seeds: CUBE +0.073/+0.160/+0.250 vs the original +0.070/+0.148/+0.255, `examples/router_fe6c`): the neighbour-**length** component was the dominant cube-locked landscape term and is **fixed** (true great-circle distances, 2026-07-24); killing diagonals in the drainage **fan** is landscape-benign; diagonal treatment in the **receiver/incision tree** is landscape-consequential (CUBE +0.07 to +0.26) and a naive D4 tree is itself not cube-safe. The owed FE(6)(c) pricing has since run — see (6)(c) — and the strawman's swing is measured to be the D4 tree's four fixed azimuths, not diagonal-removal as such.

## Epistemic Status

**Max attainable: exact** for the measurements under their harness (`crates/vivarium-world/examples/curl_probe/`, identity restatement in its module doc; pre-registered predictions and router-spike records graduated to `.super-archive/from-msc/spike-curl-probe/` and `.super-archive/from-msc/spike-principled-router/`; `examples/router_lab/` live). **Currently `exact` as observation** of the live router's defect and of the null-test structure — with the FE(5) bound: $\kappa$ convicts the fan-weighted direction, not the reconstructed physical vector. The remedy stack (FE 6) and its findings are **council-accepted** — DECISIONS family `routing-violates-the-potential-identity…`, `the-router-is-a-scalar…`, `mfds-output-is-not-a-discharge…`, all `:by claude`, `:status council-accepted` (2026-07-24: findings + the composed remedy stack — p=1 · kill diagonals · Coatléven vector reconstruction · non-orthogonality gradient — accepted as the recommended path). **Adoption of the remedy stack in production is separate work and remains open** — the router itself remains open ( #form-grid-equiangular-staggered FE(4)); the landscape-severity pricing landed as FE(8), and the owed next pricing is FE(6)(c)'s vector reconstruction on the receiver/incision tree; the diagonals-kill / vector-reconstruction / gradient steps are unbuilt. Priority: correct work, not critical-path — erosion-carving is a Phase-3 gate and `emerged-land` is the queue head (`check-the-ladder` lineage).

Stage `draft`.

## Discussion

Two independent lessons ride on one measurement. First, the identity itself had to be corrected before it could convict anything — a probe built on the brief's "curl-free" would have convicted a perfect router; the LEM survey reached the same correction independently from the literature. Second, the natural probe location was an exact null test — the same species as a green probe sampling below the fault frequency ( #norm-probe-sensitivity ), demonstrated rather than predicted. Together with the fan and uniform-area biases ( #obs-cube-locked-kernel-bias ) this completes the measured catalogue of cube-locked routing defects: attractor fan, fake erodibility, and manufactured spiral.

## Working Notes

- **Instruments:** `crates/vivarium-world/examples/curl_probe/` (identity restatement in `curl.rs` module doc) and `examples/router_lab/` — both live and re-runnable; the pre-registration artifact and raw spike records are provenance in `.super-archive/from-msc/`.
- **Self-catch preserved as regression guard:** true-width quadrature was non-deterministic run-to-run (HashMap-order branch cut); its first, flattering numbers were the bug. Re-run new probes 3× before believing them.
- **The CUBE differential has a null control as of 2026-07-29** — two D4-symmetric footprints over which every arm returns zero. Floor: RMS-CUBE below ~0.01–0.03 is indistinguishable from nothing. Earlier records quoting "fan-diagonal kill is landscape-benign, CUBE −0.02 to −0.04" were **at** that floor, not below it — a Limits list is where an unmeasured *floor* goes to look like an unmeasured *range*.
- **Do not re-use `router_pricing.rs`'s `terrain()`:** its band-limited detail term is identically zero (level-equality by construction) and seed 0 has no band-limited relief at all — a smooth radially-symmetric dome makes the router the only symmetry-breaking present, the face-centre-cone hazard wearing different clothes. On that degenerate surface the FE(6d) arm cleanly told the *opposite* story (z = 22) before the terrain was corrected.
- **Do not re-claim:** "gravity-driven flow is curl-free"; "fixing direction fixes magnitude"; the moment tilt as the remedy (superseded — wrong object); the 5.17%-vs-20.76% headline without the null-test caveat.
- **Do not re-raise $p/q \neq 1$ "for flow concentration"** — the recorded answer to that standing pressure: the (12)–(13) post-process **cannot save a $q \neq 1$ scheme** (Coatléven & Chauveau §5, verbatim: consistency "lost again"; the field's habit of re-tuning $q$ per grid size is the confession that it absorbs discretisation error, not physics). The consistent substitute exists: concentration as $\lVert\nabla h\rVert^{p_w}$ with $p_w = q-1$ — a per-cell scalar on the *full* gradient, mesh-independent, chosen on physical grounds — while direction comes from the reconstructed vector. One number was doing two jobs; split, both become principled.
- **The fan's modified equation (leading order):** $\Delta(\psi) = -A\sin(8\psi)$ with $A = 0.332°$ at $q{=}1.1$ (one mode, 94.2% of deflection variance; $A \equiv 0$ at $q{=}1$) — an odd, advective, first-order term at the fan's octagonal wavenumber. This is the *mechanism* behind the level-independent drift and converging spread the segments carry as bare measurements (router spike DERIVATION §4.2a; water-side analogue at #detail-nomos-defect-anatomy ).
- **Identifiability, run 2026-07-29 (`examples/identifiability`) — no-go on the router half, sharp on the water half.** Our router has **no conveyance term** (`accumulate_drainage` weights are normalised $(\mathrm{drop}/\mathrm{dist})^P$), so the "uniform $k_m$ is inert" claim has no referent in it and cannot convict; the natural proxy (uniform gain on the driving field) is **not** a proxy, because `drainage_surface` takes base level from an *absolute* datum — rescaling relief moves the boundary condition (×2 flips 9216/9216 submarine cells). **The live instance is `manning_n` in `water.rs`, where it holds sharply:** steady discharge inert in roughness to $10^{-4}$ while depth and velocity carry it at Manning's exponents — roughness is invisible to routing and fully visible to the carve.
- **Do not re-open the D-∞ moment-design family** (dispersion-too-low vs direction-bias, "separable and nobody has separated them"): superseded by the FE(5) dissolution — the Coatléven reconstruction yields direction and magnitude from one object, and the oracle rows measured that a *perfect* direction estimate moves scatter error only 5.17→5.16%, so direction quality was never the binding term. Judgment call recorded here so the question is owned, not silently dropped.
