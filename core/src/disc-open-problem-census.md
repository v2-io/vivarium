---
slug: disc-open-problem-census
type: discussion
status: discussion-grade
stage: draft
depends:
  - scope-segment-canon
  - form-three-scoped-runtime
  - sketch-detail-abstract-reversion
  - form-column-control-volume
  - form-grid-equiangular-staggered
  - sketch-nomos-declaration-boxes
  - obs-coarse-only-closure-nogo
  - obs-routing-curl-spiral
  - form-builder-admission
  - form-time-indexed-stage-chains
  - form-complete-content-addressed-key
  - form-core-view-wall
  - obs-cube-locked-kernel-bias
---

# Open-problem census (derived from core)

A present-tense list of **named open work** derived from `core/OUTLINE.md` gaps and segment open residues — not a ranked superlative, not a trustable count of "all research," and not ARCHITECTURE §8's retired inventory. Refresh when OUTLINE or Critical opens change.

## Formal Expression

1. **Rule.** Open-problem lists are untrusted until **derived from core** (Joseph, 2026-07-13; ARCH §8(c)). This segment is that query-shaped census for *named gaps and open residues already owned as such in core*. It does **not** claim completeness of unknown unknowns.

2. **OUTLINE gaps (missing segments).**
   - Epistemology ladder / weak-tier-in-canon  
   - Strengthen-before-soften as vivarium segment  
   - Full ETHICS segmentation  
   - Full Terrestris phase-content segments; kingdoms/orders/ordinum segment homes  
   - **Run-modes carve** (thin)  
   - Full builder daemon (beacon / demand spool)  
   - **The operated instrument** — §III is titled "Runtime, machine, and CLI" and owns no CLI/observability row: build parameterization sits on the global command rather than the manifest ( #form-core-view-wall FE(4)), `watchpoint` has no LEXICON entry, and "legible, steerable time regimes" (in-world clock vs wall clock, sim rate vs framerate) has no home. **The explorer-intent block binds here** (Joseph, 2026-07-02; re-affirmed as binding intents 2026-07-29): deepen the sediment loop (suspension/sealing/deposition) · reconcile column state with the strata Column · fine nearby water correctly *seamed* to coarser tiers (the nested water telescope) · HUD clarity + toggleable legend · water transparency · pawn visible under deep water · WASD step/speed precision · water visualization vs velocity/pitch/suspension · ground visualization vs column state (saturation, exfiltration) · wet-ground slumping (saturated banks fail) · the time-regimes item above. **Caveat carried with the re-affirmation:** the list predates the mature ethereal/participant carve and may carry game-era framing (a "pawn" presumes a participant avatar) — each item is re-read against #form-core-view-wall and #norm-no-depiction-without-referent at design time, not treated as settled spec  
   - Hydrosphere / climate / erosion / water field nomos segments beyond box + sea-level  

3. **Owned open research / compliance (segments already name them).**
   - **Nonlinear closure for a non-local flux** (law half) — #sketch-detail-abstract-reversion ; state-half up-invalidation measured solved. The coarse-only pointwise no-go ( #obs-coarse-only-closure-nogo ) is **kernel-era-scoped as of 2026-07-29**: measured on the 07-24 kernel ($R^2\le0.36$, isotonic ceiling 0.401) and **inverted on current main** (0.940/0.982, verified twice — drift isolated to ~12 commits incl. the craton rewrite) — the pointwise family is live again on today's kernel, and understanding *why* the drift re-opened it (smoother basins? fill-restore? craton coherence?) is itself an open question; the network-topology carrier and sub-grid summary remain un-excluded either way.  
   - **Three-scoped build inventory** — spine / demand spool / flux-BC tiles / time-in-every-key enforcement unbuilt under #form-three-scoped-runtime
   - **Same-level halo exchange — production datum path shipped** (2026-07-29): builder face/beacon Jacobi region sweeps + load prefers halo ( #form-same-level-halo-exchange ); still open: flux half, spill-level scalar, cube-edge $d\ge 2$, FE(7) re-measure under production keys
   - **Column semantics fork** (point / mean / band-limited) — open on #form-column-control-volume ; `Statistic::Undeclared` on registry  
   - **Router successor** (edge fluxes; kill diagonals; p=1 line) — open under #obs-cube-locked-kernel-bias / #form-grid-equiangular-staggered / #obs-routing-curl-spiral . The FE(6c) reconstruction is now **priced** (2026-07-29): cube-safe (3× over the strawman) but not more cube-safe than the diagonal-kill-plus-transmissivity arm on the live D8 tree, so orientation is no longer its argument; affordability (1.06×) and halo (2, the reconstruction's price) both measured. **Remaining opens:** the **magnitude half** — restate stream power in specific-catchment-area form with re-tuned erodibility (the confounded `CoatMag` arm is not a result); the **endorheic $s_K{=}0$ case** (carve-out measured inert on domes with sinks; untested where Priority-Flood output dominates); MFD's two-pass routing cost (still "the obvious next spike"); and the **solid-angle precision re-key rider** — land Van Oosterom–Strackee with the next deliberate kernel re-key ( #obs-cube-locked-kernel-bias WN). Cross-strand: `router_fe6c`'s `build_geom` implements exactly the four edge quantities the GCL acceptance test awaits (`DECISIONS[the-discrete-gcl-is-a-spec-not-a-defect]`) — run the acceptance against it before promoting those quantities into `measure.rs`.  
   - **Nomos box earning procedures** — fields live on `NomosDecl` and structurally tested; per-box content-earning procedures beyond shape remain open ( #sketch-nomos-declaration-boxes )  
   - **Leaf-only vs independent coarse tier** — named tension face-flux / three-scoped  
   - **Grid reopening conditions** — stand watch on #form-grid-equiangular-staggered  
   - **Recorded convergence-$\varepsilon$, and criterion-gated kernels** — #form-time-indexed-stage-chains has the claim home; nothing in the tree records the residual a stage reached, and no kernel runs to a declared tolerance rather than an authored count. Three `ASSUMPTIONS.md` rows sit **arbitrary** naming this as their cure. First step named there: water, which carries the most arbitrary constant and dominates build cost  
   - **The panel-seam O(1/h) mechanism** (opened 2026-07-29, replacing the closed GCL/ABF rows — both resolved same day, #detail-structure-scheme-map WN): the measured two-point order $-0.5$ is a conforming same-level cube-panel-seam defect, ~99.5% of the L2 norm on the seam ring while the interior converges. The obs segment is landed ( #obs-the-two-point-defect-lives-on-the-panel-seam ); open: the decisive mechanism test — freeze the metric across a band either side of a seam and see whether the growth vanishes  
   - **Water order-of-work (council 2026-07-24) — all three probes now run** (2026-07-29): clip census (the Froude cap is a thin-film clamp at 0.05–0.21%; the Jarrett ceiling is the dominant clip at 64–73%, so Jarrett is constant roughness on two-thirds of the domain; the positivity-clamp worry closes — it never fires) and the roll-wave probe, whose result **escalates to council**: the growing mode has no Froude threshold, the Vedernikov identification fails, and $\theta$ is unconditional-stability viscosity (`DECISIONS[the-growing-mode-has-no-froude-threshold-so-it-is-not-vedernikov]`, proposed — supersedes parts of two council-accepted entries; awaiting adjudication). Same strand: the shipped timestep is **friction-bound, not CFL-bound** (~8% slow, one-sided) — joins #obs-water-fill-never-settles in charging the step-size question from the opposite side  
   - **GC of superseded-by-`SRC_HASH` generations** — every lib edit orphans a whole store generation by design; nothing collects them (named open in `DECISIONS[epoch-surfaces-are-store-citizens]` impact + CONSOLIDATION; indexed here because this list is where open work is supposed to be findable). Constraint riding with it: never discard memos of a world that has hosted a mourning-capable inhabitant ( #detail-vivium-lifecycle doctrine 7) — the moment eviction lands, that ethical invariant needs a mechanism that does not exist  
   - **Assumptions reverse-guard** — the live build guard is declared→ledger only (`every_assumption_is_in_the_ledger`) with substring matching; ledger→declared (unclaimed-row detection) and row-grade matching are unbuilt and were unrecorded anywhere until this row. Forcing-function note from the design README: the ledger wants to be udon so the parser reads data, not prose  
   - **Source-digest granularity: closed as a measured no-go, listed so it is not re-opened blind** — per-nomos attribution under-keys on present code structure, and even the compiler-safe form buys 0% on the most-edited files ( #form-complete-content-addressed-key Known-incomplete (3)). The live constraint is module **separation**, not attribution derivability  

4. **Not ranked.** No "the one open problem." No import of pre-core BREAK lists as current census.

## Epistemic Status

**Max attainable: discussion-grade** as a maintained index. **Currently `discussion-grade`.** Stage `draft`. Derives only from OUTLINE + segment open language at write time (2026-07-24, post the mantle-thermal / rock-mass-ledger / mean-pin-retirement / router-pricing session); re-run the derivation after major peels. Refreshed 2026-07-24 (evening) after the harness reconciliation: the never-block store-side row closed, the operated-instrument gap and the recorded-$\varepsilon$ work added, source-digest granularity moved from open to measured no-go.

## Discussion

ARCHITECTURE §8 ordered: rename detail→abstract in core (done) and stop trusting author-ranked inventories. This segment is the replacement habit — a query over claim homes, not a mood.

## Working Notes

- Graduated ARCHITECTURE §8 is history; cite this slug for the census habit.
- When a gap closes, delete the row here (integration is replacement).
- **Ratified phase doctrine awaiting mining:** the ordinum's reportatio (`.super-archive/from-archive/PHASES.md`, pin `42621d5`) carries decided-by-joseph doctrine with no claim homes yet — canceling pairs, the phase-intrusion budget, declare-causally / materialize-lazily (incl. the corrected moon service list), phases-choose-machinery. The FE(2) gap rows say the phase segments are missing; this note says where the decided content sits, so it is mined rather than re-derived cold.
- **Adoption calls dispositioned 2026-07-29** under Joseph's delegated grant: `DECISIONS[candidates-adoption-calls-dispositioned]` — seven adopted (FORMAT §§3/5/6, #ops-audit-integration FE(4)/(6), the DECISIONS legend, #norm-physics-not-knobs), two covered-in-substance, one rejected (`:status retired` vs present-truth-only), one deferred to program level, biosphere-first mooted-for-now with its revisit condition named. Evidence trail: `msc/candidates-adjudication-2026-07-28/`.
