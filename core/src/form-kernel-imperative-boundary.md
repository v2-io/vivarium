---
slug: form-kernel-imperative-boundary
type: formulation
status: robust-qualitative
stage: draft
depends:
  - def-nomos
  - form-nomotheke-registry
  - norm-declaration-must-convict
---

# Kernel is the only imperative escape hatch

For each nomos, the **kernel** (the numerical procedure) is the sole intended imperative surface. Identity, epistemic tags, interactions, lifecycle, and coupling class are **declaration data** the audit may read without running arithmetic. Progress means extracting implicit contracts *out of* kernel bodies into the nomotheke — not rewriting numerics as a DSL.

## Formal Expression

1. **Imperative boundary.** A nomos has a **kernel** and a **declaration**. The kernel is the only intended place for imperative world-evolution code. Everything else about the article — identity, epistemic self-description, consumes/promises, conservation claims, assumptions, and (as they land) lifecycle stage, execution class, timescale band, phase binding — is **data** the registry and pre-run audits may inspect without executing the kernel.
2. **Direction of progress.** “More declarative is better” means **moving** contracts from opaque kernel bodies into declared fields so the multiscale web interlocks and audits before Rust runs. It does **not** mean encoding stream-power arithmetic in udon (a worse language for the same imperative work).
3. **Audits fall out of data.** Graph queries over declarations (requisite chain, vocabulary closure, conservation flags, weakest-link fold, and — when declared — statistic match, timescale band, execution class) are the pre-run picture of honesty. Kernels run after the graph is sound or after honesty is reported ( #form-flux-web , #form-nomotheke-registry ).
4. **Relation to convictability.** #norm-declaration-must-convict is the negative law (a declaration that cannot fail is a wish). This segment is the positive boundary: *what belongs outside the kernel so that convictable declarations can exist.*
5. **Compliance debt is named, not denied.** Fields still only in kernel bodies or design prose (execution-class, timescale-band, stage lifecycle, phase binding; source-derived versions) are residual debt under this law. Absence from crates does not revoke the boundary.

## Epistemic Status

**Max attainable: exact** as method once ratified in DECISIONS. **Currently `robust-qualitative`:** Joseph-attributed steer in the 2026-07-12 declarative-frontier / definitions spike framing (“declare everything you can; kernel is the only imperative escape hatch”) — **no dedicated DECISIONS row** found; do not invent `:by us`. Project practice (nomotheke, flux audit, refuse-unmet) already moves in this direction.

Stage `draft`. Sources: `.super-archive/from-archive/VIVARIA-DEFINITIONS.md` thesis (graduated); ARCHITECTURE §9 adding-a-system checklist as teaching.

## Discussion

Without this boundary, every new honesty check invents a one-off code path. With it, the work is to *name* the contract so the same audit machinery sees it.

## Working Notes

- Dual-home: VIVARIA-DEFINITIONS thesis → this slug; then super-archive eligibility with sibling peels.
- Do not re-import five-layer scaffold, kernel-as-DSL, or agentic “assessed” as store truth.
- Sibling: statistic match FE on `#form-flux-web`; execution-class extract from live design later.
