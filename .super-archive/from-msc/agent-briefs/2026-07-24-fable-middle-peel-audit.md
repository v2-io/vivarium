# Fable middle-peel audit (adversarial)

*Peer auditor report for parent session. Not claim canon. 2026-07-24.*

**Scope:** six peels named in the brief (plus related folds / dual-home hygiene) as they sit **now** in the tree. Truth above protecting Fable’s work or prior audits.

**Method**

- Full read of:
  - `#form-declared-structure-tradeoff`
  - `#obs-routing-curl-spiral`
  - `#form-temporal-lod-regimes`
  - `#form-material-property-interface`
  - `#form-cellid-chunk-patch`
  - `#sketch-dynamic-exponent-seams`
- Cross-read: OUTLINE §V rows; folds into `#form-fidelity-ladder`, `#form-flux-web`, `#form-seam-flux-exchange`; sibling `#form-grid-equiangular-staggered`, `#obs-cube-locked-kernel-bias`, `#sketch-nomos-declaration-boxes`
- Dual-home surfaces: `doc/theory/discretisation-and-information.md` §4–4.1a; `doc/theory/multiscale-seams.md` §3; `doc/design/{DESIGN-REDUX,DESIGN-MATERIAL,NOMOS-CONTRACT}.md`; `doc/ARCHITECTURE.md` substrate bullet
- Instruments: `DECISIONS.decision-log.udon` (routing + structure-preserving families); `crates/vivarium-world/src/chunk.rs`, `water.rs` (colmation/armor), `erosion.rs` (creep clamp), `sphere.rs` (CellId), `hydrosphere.rs` / `climate.rs` (box vs field); `msc/spike-curl-probe/PREDICTIONS.md`; `msc/spike-principled-router/{MEASUREMENTS,DERIVATION}.md`
- Integration rules applied: core = present best; retired text → history layer; Working Notes = forward residue; super-archive ≠ history of core; claim-home pointer tables ≠ integration; code = instrument/compliance, not sole warrant — **and** do not claim live what is unbuilt

**Limit (honest):** this environment had no reliable `git show`/`git log` for `40479ee` / `d9734d4`. Judgment is from **current tree content** against the brief’s named targets, not a byte-diff of those two commits. Parent can pin “what Fable fixed in d9734d4” against this report if needed.

---

## Verdict

**Mostly sound peels — keep as draft claim homes. Not dual-home-clean. Not ready to treat as claims-verified.**

The six segments are, in the main, **honest on authority, measurement vs remedy, and unbuilt debt**. The verify pass clearly left fingerprints: AL81 is right-way (both energy + PE, semi-discrete only); κ is bound to the fan-weighted object; moment-tilt is superseded; cross-face halo is named unbuilt; $z$ stayed a sketch; Joseph ratification is not inflated for agent-proposed DECISIONS.

What fails the hostile read is not a catastrophically false segment body. It is:

1. **Dual-home still carries load-bearing FE voice** (and in places **stale mid-day remedy / unbuilt-as-live**) after claim-home banners.
2. **Sibling segment bookkeeping** did not finish when the curl obs landed (`#form-grid` residual gap; `#obs-cube-locked` “still extractable”).
3. **A few present-best understatements** (armor is live; macro “chunk” tier is thinner than FE(3)’s “Live” tag implies).

No segment invents a second law home inside `core/`. No Joseph `:by us` ratification is falsely claimed on the new formulation stack. **Do not graduate the dual-home sources** on the basis of the banners alone.

---

## Severity table

| ID | Severity | Area | Finding |
|----|----------|------|---------|
| F1 | **High — dual-home / mid-day freeze** | `doc/theory/discretisation-and-information.md` §4.1 “potential” row | Still states the **first-moment condition on outflow weights** as “**the actual remedy**,” with moment-tilt numbers. Core `#obs-routing-curl-spiral` FE(5)–(6) correctly **supersedes** that object and stacks Coatléven reconstruction after kill-diagonals. Theory still freezes the **pre-spike midday** remedy as teaching that reads like law. Claim-home banner on §4 is insufficient: this is active dual-home of a **refuted intermediate**, not harmless texture. |
| F2 | **High — dual-home unbuilt-as-live** | `DESIGN-MATERIAL.md` §8 (and `ARCHITECTURE.md` substrate bullet; `chunk.rs` crate docs) | Design still: *“Halos carry cross-chunk and **cross-cube-face** neighbours … filled at patch load.”* Segment FE(4) correctly: **same-face live; cross-face designed, unbuilt** (`fill` leaves out-of-face at default). Crate module doc claims cross-face filled at load; `fill` doc contradicts. **Segment honest; dual-home + code commentary overclaim.** |
| F3 | **Medium — dual-home FE restatement** | `DESIGN-REDUX.md` §3 | Claim-home banner, then full normative restatement of the four regimes + lag condition (the segment’s FE nearly line-for-line). Teaching examples would be fine; **regime taxonomy as second FE** is dual-home cosplay under Joseph’s integration rule. Same class (lighter): DESIGN-MATERIAL §§5–6 property/ladder prose after banners. |
| F4 | **Medium — false / stale present-best in dual-home + Working Notes** | Material interface / armor | `DESIGN-MATERIAL` §5 still: armoring *“Joseph’s list, **not yet modeled**.”* Live `water.rs` has `armor` / `armor_depth` / `armor_shield` and updates them in the step. Segment Working Notes: *“colmation live … **armoring named**”* — **understates**. Schema-home residual is real; “named not live” is not. |
| F5 | **Medium — incomplete sibling fold** | `#form-grid-equiangular-staggered` FE(5); `#obs-cube-locked-kernel-bias` Working Notes | Grid FE(5): *“Related routing curl/potential-identity **still a residual gap**.”* Cube-locked notes: sibling curl *“**still extractable** as its own obs if needed.”* **`#obs-routing-curl-spiral` exists and is OUTLINE-wired.** Residual-gap language is false **now**. |
| F6 | **Medium — FE live tag overreach** | `#form-cellid-chunk-patch` FE(3) | Two size tiers stated; “**Live:** `chunk.rs` (patch + halo).” Code has `Patch`, not a macro streaming `Chunk` citizen. Epistemic Status admits macro tier thinner — **good** — but FE(3)’s Live tag still sells the pair. Tighten FE(3) to *patch live; macro tier design*. |
| F7 | **Low–medium — sketch depends hygiene** | `#sketch-nomos-declaration-boxes` | Box ④ is structure (preserved/sacrificed); `#form-declared-structure-tradeoff` now owns that law and points to the sketch. Sketch depends do **not** include `form-declared-structure-tradeoff` (still only flux-web / prime-question / …). Asymmetric wiring. |
| F8 | **Low — OUTLINE Critical vs schedule prose** | `#obs-routing-curl-spiral` | OUTLINE: **Model Critical**. Body: *“correct work, not critical-path.”* Not a contradiction if Critical = model load-bearing, not build queue — but a later agent will misread. One clarifying clause in Epistemic Status or OUTLINE aspect note would help. |
| F9 | **Low — dual-home authority color** | `multiscale-seams.md` §3 | Sketch correctly: Joseph steer, **no DECISIONS row**. Source still: *“Joseph’s steer … **endorsed**.”* “Endorsed” without a DECISIONS row is soft authority inflation in the dual-home. Prefer “steer recorded in design prose.” |
| F10 | **Info — folds claimed in brief** | `#form-fidelity-ladder` / `#form-flux-web` / `#form-seam-flux-exchange` | Ladder FE(6) scaffolding (armor/colmation demolition date) is real and clean. Flux-web FE(6) statistic and seam FE(7) f32 look like **prior** peels, not this middle-peel’s main payload. No defect — just don’t credit this wave with inventing them. |
| F11 | **Info — method** | This audit | κ headline numbers (~2%, face-centre null, level-independence) matched DECISIONS + theory + segment; **probes not re-run**. Router MEASUREMENTS.md confirms phantom flux 47.8%, p=1.1 → 0.2419°, identity residual order. Parent should treat measurement claims as **document-chain verified**, not re-executed. |
| F12 | **Info — commits** | `40479ee` / `d9734d4` | Not re-diffed here. Current state is the audit object. |

---

## What Fable already fixed well

These look like the verify cycle did real work (or the peel was already disciplined). **Do not re-soften these.**

| Area | Why it holds |
|------|----------------|
| **AL81 / structure conflict** | `#form-declared-structure-tradeoff` FE(2) and theory §4.1a: square C-grid can do **both** energy + PE **semi-discretely**; Ringler/TRiSK cannot on arbitrary meshes; time integrator is the sacrifice. Matches `DECISIONS[the-grid-recon-argues-FOR-the-quad-grid]`. No false “one but not both” on AL81. |
| **κ object bound** | Obs FE(5): probe convicts **fan-weighted transport direction**, not $\hat Q_K$. Matches `the-router-is-a-scalar…` / `mfds-output-is-not-a-discharge…`. Prevents “fixing direction fixes magnitude.” |
| **Remedy stack honesty** | FE(6) ordered (p=1 → kill diagonals → Coatléven reconstruct → non-ortho gradient separate); moment-tilt **superseded instrument**; stack **proposed**, `:by claude`. Severity **unpriced** until channel-network experiment. |
| **Null-test / probe sensitivity** | Face-centre cone as exact null; depends on `#norm-probe-sensitivity` / `#norm-bias-vs-noise` used correctly, not ornamental. PREDICTIONS.md written-before-run still present. |
| **Authority discipline** | Structure declare-three-lists: **no Joseph ratification** stated. Temporal LOD / material interface / hex decline: agent design stance, not inflated to decided. Sketch $z$: explicitly not DECISIONS. |
| **Code vs unbuilt (in segments)** | CellId/patch: live + known incomplete. Cross-face halo: segment correct. Material schema: TENTATIVE / not claimed. NomosDecl structure fields: named build debt. Water $z=1$ vs creep clamp $z=2$: matches `stable_dt` / `k.min(0.24)` reading. |
| **Four-family census** | Staggered water, collocated creep, graph router, box hydrosphere + climate box→field — consistent with live crates as *reading of stack*, not “one FVM.” |
| **Seam / sketch boundary** | `#form-seam-flux-exchange` keeps $z$ as coinage; sketch owns exploratory tactic. No second seam law. |
| **Working Notes shape** | Mostly forward residue + dual-home demote notes + do-not-reclaim lists. Not history dumps of the audit day. |
| **OUTLINE wiring** | All six rows present; order after grid / cube-locked / seam family is sensible; depends generally respect earlier OUTLINE homes (`form-seam`, `form-grid`, `form-pull-query`, `form-fidelity-ladder`, …). |
| **Integration-as-replacement (core bodies)** | Segment bodies state present best; superseded remedies are marked superseded **inside** the obs, not kept as soft ghosts with “not a weakening.” |

---

## Segment-by-segment (hostile)

### `#obs-routing-curl-spiral` — **keep; strongest of the six**

- **Status `exact` as observation** of measured defect + null-test structure is earned *if* the harness still matches DECISIONS (document chain OK; not re-run).
- Remedy and adoption correctly **not** exact.
- FE(5) bound is load-bearing honesty — protect it; do not let discussion re-expand κ into “physical flow direction.”
- Residual: sibling links (F5); dual-home theory row still advertising first-moment remedy (F1).

### `#form-declared-structure-tradeoff` — **keep as robust-qualitative**

- Principle + conflict + linear-only seam crossing is a clean peel of theory §4 / DECISIONS structure-preserving family.
- Honest about rediscovery / proposed DECISIONS / no Joseph standing-law tag.
- FE(4) live-family census is observation-grade reading of crates — OK if not re-read as “architecture decided four families forever.”
- Dual-home: theory §4 still **re-states the governing principle as imperative law** under a claim-home banner (F1 adjacent). Demote banner is not demotion of voice.

### `#form-cellid-chunk-patch` — **keep; tighten Live tags**

- CellId packing, Hilbert-for-containers, Cartesian interior: matches `sphere.rs` / `chunk.rs` / bench rationale.
- Cross-face unbuilt: **segment correct** (F2 is dual-home’s fault).
- FE(3) Live overclaim (F6); hex decline correctly non-DECISIONS.
- Status `exact` for *address architecture in force* is defensible with the Known incomplete list — only if FE(3)–(4) stay precise.

### `#form-temporal-lod-regimes` — **keep as robust-qualitative design stance**

- Regime taxonomy matches DESIGN-REDUX §3; open non-locality limit kept.
- Honest: no Joseph DECISIONS row; regime count not closed.
- Open tension with leaf-only / face-flux-register correctly in Working Notes, not smuggled into FE as solved.
- Dual-home F3 is the main remaining defect class.

### `#form-material-property-interface` — **keep; fix armor present-best**

- Interface-stability architecture + ladder + undifferentiated prefix: good robust-qualitative peel.
- Survey residual honesty (missing property members) kept.
- Armor / interface-state: F4 — fix Working Notes and dual-home source in the same cycle.
- Scaffolding fold into `#form-fidelity-ladder` FE(6): good, minimal, not dual-home.

### `#sketch-dynamic-exponent-seams` — **keep as sketch; right type**

- $z=1$/$z=2$ facts standard; clamp reading code-true; tactic not over-promoted.
- Depends on cellid + seam + scale-separation: honest.
- Source multiscale-seams still long teaching (OK) with soft “endorsed” (F9).

---

## Dual-home hygiene (what “demote” actually did)

| Source | Banner? | Residual problem |
|--------|---------|------------------|
| theory §4 / §4.1a | Yes → declared-structure | Full governing principle + **stale first-moment remedy** in structure table |
| DESIGN-REDUX §3 | Yes → temporal-lod | Full four-regime FE restated |
| DESIGN-MATERIAL §§5–6 | Yes → material | Property/ladder FE restated; **armor “not yet modeled” false** |
| DESIGN-MATERIAL §8 | Yes → cellid | Cross-face halo stated as if built |
| NOMOS-CONTRACT | Yes → declared-structure / sketch boxes | Defect table as teaching — OK if not re-cited as law |
| multiscale-seams §3 | Yes → sketch $z$ | Long teaching retained; “endorsed” color |
| ARCHITECTURE substrate | Partial / none for cellid | Halos “cross-face” as settled |
| `chunk.rs` module docs | n/a | Overclaim vs `fill` |

**Rule application:** banners + OUTLINE rows are **not** integration. Integration would make **core the only place an agent can read present-best FE**, with dual-home reduced to specimens, tables, and instruments that cannot be mistaken for law. Present state is **partial peel**.

---

## Depends / OUTLINE (no critical cycle found)

- New rows sit in OUTLINE §V after grid / cube-locked / seam algebra — correct neighborhood.
- `form-declared-structure` → `form-seam-flux-exchange` (earlier): OK.
- `form-cellid` → `form-grid` (earlier): OK.
- `form-temporal-lod` → `form-pull-query-composition` (Machine §, earlier): OK.
- `form-material` → `form-fidelity-ladder` / `form-flux-web` (earlier): OK.
- `sketch-dynamic` → `form-cellid` (same section, earlier row): OK.
- **Missing edge:** sketch-nomos ④ ↔ form-declared-structure (F7).
- **Stale residual language:** form-grid FE(5), obs-cube-locked Working Notes (F5).

---

## Recommended parent actions (cheap → structural)

### Do now (same cycle if fixing)

1. **Theory §4.1 potential row (F1):** replace “first-moment is the actual remedy” with: (a) measured defect pointer → `#obs-routing-curl-spiral`; (b) remedy stack **proposed** as in obs FE(6); (c) moment-tilt as **historical instrument only** (or delete from the table’s “what preserves it” cell entirely). Present best lives in core; theory table must not re-convict agents into the wrong fix.
2. **Cross-face halo (F2):** one-line honesty in DESIGN-MATERIAL §8, ARCHITECTURE substrate bullet, and `chunk.rs` **module** docs — match segment FE(4) / `fill` docs.
3. **Armor present-best (F4):** DESIGN-MATERIAL §5 + form-material Working Notes: both colmation **and** armor live in `water.rs`; residual = **column interface-state schema home**, not “unmodeled.”
4. **Sibling bookkeeping (F5):** form-grid FE(5) → cite `#obs-routing-curl-spiral`; obs-cube-locked Working Notes → “extracted as `#obs-routing-curl-spiral`.”

### Should soon

5. **DESIGN-REDUX §3 (F3):** keep 1–2 teaching examples; delete or compress the four-regime FE clone so an agent cannot cite REDUX as co-equal law.
6. **form-cellid FE(3) (F6):** Live = patch+same-face halo; macro chunk = design tier.
7. **sketch-nomos depends (F7):** add `form-declared-structure-tradeoff` (or reverse note only — pick one directed depends story).

### Do not

- Do **not** status-up any of the six to claims-verified until F1–F2 dual-home and F5 siblings are fixed (or parent explicitly accepts dual-home residual as mine debt).
- Do **not** adopt the routing remedy stack as decided — still `:by claude` proposed; pricing experiment unpaid.
- Do **not** treat claim-home banners as super-archive graduation criteria for these design/theory files — residual FE voice and stale mid-day content remain.

---

## On the verify pass (inferred from state, not git)

What looks **already corrected well** (likely d9734d4 or careful first peel):

- AL81 direction and semi-discrete caveat
- κ object / Helmholtz split / null test
- Moment-tilt supersession + Coatléven stack ordering
- Authority tags and “no Joseph DECISIONS” on stance peels
- Cross-face unbuilt **inside** `#form-cellid-chunk-patch`
- Sketch vs formulation for $z$

What the verify pass **missed or left for dual-home cleanup**:

- Stale first-moment remedy in theory table (F1)
- Unbuilt-as-live halo language outside the segment (F2)
- Armor “not modeled / named only” (F4)
- Sibling residual-gap language after extraction (F5)
- Full FE clones under banners (F3)

That pattern matches prior vivarium peel failure modes: **core body careful; dual-home demotion incomplete.**

---

## Stay available

I can, on parent ask:

- Draft the F1–F5 patch text (theory row, DESIGN-MATERIAL §5/§8, form-grid FE(5), cube-locked notes, material Working Notes) without expanding scope
- Re-check any single primary cite (AL81 / Ringler / Coatléven) against `msc/spike-principled-router/refs/` or relata if shell/PDF read is available
- Diff `40479ee`/`d9734d4` if `git show` becomes available and parent wants commit-attributed credit vs residual

Not claiming comprehensive literature re-read of ~45 structure-preserving primaries — only that **segment claims about AL81 / linear-only seam / Coatléven composition match the DECISIONS + spike document chain**, and that **dual-home present-best is the main live defect class**.
