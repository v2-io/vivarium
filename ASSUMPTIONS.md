# vivarium — ASSUMPTIONS (the magic-constant ledger)

> **Why this file is still at the root, when every other front-door document was archived on 2026-07-13.**
>
> It is not a front door — nobody reads it to get oriented — and it is load-bearing *in code*: `crates/vivarium-world/src/nomotheke.rs` does `include_str!("../../../ASSUMPTIONS.md")`, and the `every_assumption_is_in_the_ledger` test fails the build when a nomos declares an anchor this file does not carry. That guard is one of the few things in the repo that actually convicts, so the file stays until the mechanism can move intact rather than being broken in transit.
>
> **It is not exempt from the new discipline. It is in debt to it.** Two things are owed:
>
> 1. **The register needs scrubbing.** Much of what follows argues in emphasis rather than in claims — bold, capitals, ⛔. The measurements underneath are real and were expensive; the typography is not evidence, and an absolute is a claim with no predicate. See `FORMAT.md` §6 "No absolutes".
> 2. **It should not be prose.** A hand-kept markdown table is exactly the artifact class that goes stale, and this file already says so in its own opening: the ledger *"becomes generated, not hand-kept"* once each nomos carries its declaration as data. The destination is **udon**, with proper tooling, when the udon-util crate is ready — at which point the ledger is generated, and this file retires to `.archive/` with the others.
>
> Until then it is authoritative for what it records, and the `include_str!` guard stays live.

*The canonical accounting of every value the world runs on that is not yet principled — created 2026-07-10 at Joseph's direction: "there needs to be a standard canonical place where these things must be all accounted for, so that we know exactly what assumptions are still being filled in unprincipled ways and we don't end up with guesses and magic constants (even fated ones or steered) scattered through the nomos making it unlawful and **undiscoverably unlawful**."*

**The rule.** No numeric constant enters a nomos without an entry here (or a doc-comment pointing here). An unprincipled value is not the sin — *an unaccounted one is*: declared placeholders are honest rungs (LEXICON §5's D-on-low-B made glaring instead of camouflaged); undeclared ones are latent unLawfulness that no probe will ever localize. This file is the prose bridge until the declared-vs-derived machinery (LEXICON §5) makes each nomos carry its own declaration as *data* — at which point this ledger becomes generated, not hand-kept.

**Status vocabulary:** `earth-ref` (taken from measured Earth — principled *by reference*, jettisonable for non-Earth worlds) · `literature` (a published model/value used as intended) · `tuned` (chosen to look/behave right; documented reasoning, no process) · `arbitrary` (a placeholder someone had to pick) · `operational` (affects builds/views, never world-law — listed only when confusable).

## World-law constants (in nomos — these shape every world)

| constant | value | lives at | status | principling path |
|---|---|---|---|---|
| `SEA_LEVEL_M` (retired as land gate) | was 4000 m decreed; land/water now uses `sea_level::derived_sea_level_m(seed)` | `gen.rs` (compat residual), `sea_level.rs` | **derived** (pour ocean into tectonic hypsometry) | ⛔ **2026-07-12 probe** convicted the decreed datum as manufacturing forbidden land. **2026-07-23 code-first:** pour is live (`derived_sea_level_m`); bathymetry prior has no freeboard; freeboard is uplift's. Constant kept only for migrating call sites — not for classification. |
| ~~`freeboard amplitude`~~ | — | ⛔ **RETIRED 2026-07-24** | — | The fBm freeboard stand-in is replaced by the mass-conserving isostasy read of the lithosphere column (`lithosphere.rs`; `#form-isostasy-column`). See the three lithosphere rows below. |
| `mantle potential temperature` | 1550 °C present-Abyssal anchor (Archean-hot; modern ref 1350 °C) | `lithosphere.rs` (`MANTLE_TP_C`, `TP_MODERN_C`), the `lithosphere` nomos | **earth-ref era pin** | The secular-cooling driver: sets oceanic crust thickness (~30 km hot → 7 km modern) and density (~2850 → 2950). Cooling deepens basins and grows land — column-side monotonicity unit-tested. Now the *present-Abyssal anchor* of the mantle-thermal cooling trajectory (row below), no longer a bare constant. |
| `mantle cooling trajectory` | present-Abyssal age ~3.2 Ga; relaxation $\tau$ ~1.07 Ga; Hadean ceiling 1650 °C | `mantle_thermal.rs` (`PRESENT_ABYSSAL_GA`, `COOLING_TAU_GA`, `TP_HOT_MAX_C`), the `mantle-thermal` nomos | **declared crude** (order-of-magnitude; Korenaga 2017 unread) | The secular-cooling process as an article of law: `T_p(t) = TP_MODERN + (MANTLE_TP_C − TP_MODERN)·exp((age − present)/τ)`, monotone in time, through the present anchor toward the modern asymptote. The SHAPE of emergence (cooling ⇒ deepening basins ⇒ growing land) is convictable (`mantle_cooling_probe`, monotonicity test); the rate/timing is declared crude, no calibration claimed. Clamped to `[1350, 1650]`. |
| `lithosphere densities` | asthenosphere 3300, felsic crust 2750, keel deficit 25 kg/m³; oceanic ramp 2850–2950; sediment 2500 kg/m³ | `lithosphere.rs` (`RHO_*`, `RHO_SEDIMENT`) | **literature-order** (Chowdhury 2025 lineage; not re-derived in-repo) | The Airy balance these feed is real physics; the values are era-plausible pins. Keel deficit is small but acts over 180 km — keel-share of craton stand unit-tested >15%. Sediment (lighter than felsic crust) is the redeposited-erosion blanket of the rock-mass ledger (`erosion_return.rs`); a buoyant load, so basins that receive it rebound down. |
| `erosion maturity` | $\varphi$ = 1.0 (fraction of subaerial stand converted to crust removal per ledger pass) | `erosion_return.rs` (`EROSION_MATURITY`), the rock-mass ledger | **declared crude** (physics Low; stand-in for ∫stream-power, uncalibrated) | One pour-grain erosion step: crust removed ∝ stand above sea, isostatic rebound automatic (~83% of felsic crust returns as uplift). At $\varphi=1$ the amber over-stand trims ~17% (seed 0 2436→2025 m, seed 7 2386→1982 m into band) — an observation, not a tuned target. Iterated epochs and routed deposition are open rungs (`#form-isostasy-column` FE(9)). |
| `craton geometry` | crust 35 km, keel 180 km, present area fraction ~0.12; nucleation-growth: 8–13 blue-noise sites (min sep 0.22 rad), mean accretion radius 0.235 rad × [0.6,1.5] spread, margin 0.06 rad, boundary warp 0.22 @ f2.0, growth saturation 4.0 | `lithosphere.rs` (`CRATON_*`, `craton_sites`, `craton_weight_at_tp`) | **arbitrary** stand-in for the differentiation rate law, horizontal half (open modelling) | Fated **nucleation-and-growth** cratonization — a few nuclei accreting coherent cratons of a characteristic scale that GROW as the mantle cools, NOT a threshold slice through scale-free fBm (percolation speckle, the retired field). Thick felsic crust + depleted keel as one process's two products. Present area CONVICTED by `craton_fraction_is_in_band` (seed 0 ~0.093, in band); morphology (few coherent cratons, not speckle) CONVICTED by `craton_morphology` at a matched land budget (fBm fragments ~2.3–2.8× more). Land fraction after pour stays in the Abyssal band (sea_level test). |
| continental band $\lambda$, amplitude | ~1250 km, ±1500 m | `gen.rs` (`CONT_PER_FACE`, ×3000) | **tuned** — and phase-structurally wrong (it impersonates Abyssal output) | ⛔ Single-population fBm is UNIMODAL by construction: no deep basins, no high platforms. But **bimodality is NOT the fix here** — isostasy (two crust densities floating at different heights) is the **mechanism by which Abyssal DELIVERS land**, so it belongs to the `emergent-land` charge, *not* to the initial condition. A two-mode crustal *prior* would manufacture exactly the land the ordinum forbids as an initial condition. **Principling path:** this band becomes honest **bathymetry** (seafloor relief over seeded-asymmetry crust) and the `uplift` nomos earns the land. The early-continents survey §6 targets (land 2–15%, compressed bimodal, relief 3–5 km) are the **mid-Abyssal verification target those processes must PRODUCE** — a Record-style check, never an initial condition. *(⚠ open, Joseph: the ordinum does not strictly forbid early emergence — a mismatch is a SIGN that either the model or the ladder is wrong; the papers adjudicate. TODO §#1 gap.)* |
| mountain band $\lambda$, amplitude, gate | ~25 km, ±1800 m, on cont>−200..+600 m | `gen.rs` (`MTN_PER_FACE` etc.) | **tuned** (core-proven scaling) | superseded by the erosion tier earning the relief |
| fBm shape | 4 octaves (cont), lacunarity 2.0, gain 0.5, octave-domain stride 0x9E37 | `gen.rs`/`noise.rs` | **arbitrary** (standard defaults) | irrelevant once noise is only sub-grid texture |
| soil mantle | 2.0 m everywhere | `gen.rs` `baseline_column` | **arbitrary** | pedogenesis system (DESIGN-SYSTEMS row) |
| soil saturation | 1.0 below sea / 0.3 above | `gen.rs` | **arbitrary** | groundwater/Darcy tier |
| stream-power `m` | 0.5 | `erosion.rs` defaults | **literature** (standard SPIM exponent) | per-material + threshold (Lague 2014 caveat already in survey). ⚠ Note `n = 1` is **not** a free choice: the Braun–Willett implicit solve is exact *because* $n=1$ — so stream power is **linear in slope**, and the coarse-graining bias lives in **$A^{0.5}$ and $\mathrm{Cov}(A^m,S)$**, not in $S$ (`DECISIONS[the-jensen-variable-was-wrong-and-so-was-the-sign]`) |
| **MFD flow exponent `P`** | **1.0** (was 1.1) | `erosion.rs` (`const P: f32 = 1.0`) | **principled (directional first moment)** on a square lattice | **2026-07-24:** live value set to $p=1$ per theorem + measurement (`DECISIONS[the-router-is-a-scalar-pretending-to-be-a-vector-and-p-is-the-bias]`, council-accepted). At $q=1$, weights $\propto \cos^q$ have **exactly zero** first-moment deflection on a perfect square lattice; at $q=1.1$ the same control printed **rms ~0.24°** eight attractors (was misread as "intrinsic MFD error"). **Residual (not retired by $p$):** MFD fan as sheared quadrature / diagonals / Coatléven vector reconstruction — `#obs-cube-locked-kernel-bias`, `#obs-routing-curl-spiral`, `.super-archive/from-msc/spike-principled-router/`. |
| `drainage cell area` | **true spherical** per cell (drainage seed + deposit); MFD neighbour **lengths** still uniform `cell_m` | `erosion.rs` (`cell_area` vec + `measure::cell_area_m2`); lengths via `cell_m` | **partial** — area bias retired for $A$ accumulation; length residual open | **2026-07-23 PoC:** drainage/deposit use equiangular closed-form area (`DECISIONS[drainage-area-uses-a-uniform-cell-area]` council note: partially executed). Historical measurement of uniform-$A$ bias (+17.8% mean) remains the conviction for *why* the fix mattered (`#obs-cube-locked-kernel-bias`). **Still open:** true edge lengths / kill diagonals / router successor. |
| erosion `k_dt`, deposition G, talus slope, κ | 0.02, 1.0, 0.8, 2 m²/epoch | `erosion.rs` defaults | **tuned** (probe-checked behavior) | calibrate against real denudation rates when epoch↔years is pinned |
| epoch ↔ years | `EPOCH_YEARS=100` nominal | worldview HUD | **arbitrary** | component E (time-indexed stages) must pin it |
| erosion run length | `epochs=40` (CLI/store_explore), 80 default | callers | **arbitrary** — and **measured 2026-07-24 to be wrong in both directions at once**: on subaerially-empty tiles (3 of 4 sampled L9 footprints, 0–5 land cells of 4096) the kernel does *nothing* and the epochs buy only accumulated uplift; on the tile that does erode, 40 sits ~36× above its epoch-400 residual and is still falling. `#obs-erosion-residual-is-driver-bound`, `examples/erosion_settle_probe` | **not** a convergence-$\varepsilon$ gate — that form is a measured no-go here, because sustained uplift pins the residual at the driver's rate and leaves no stationarity to detect. Retired by the **a-priori response time** (`Fluvial::response_census` — first measurement 2026-07-28: channel network crossed in ~20–70 epochs at L9, ~300 at candidate L13 patches; the criterion-grade χ-shape test remains open) for working tiles, and by a **no-subaerial-cells precondition** (converged at epoch 0, checkable before running) for the inert majority |
| erosion stage stride | every 5 epochs (manifest demand default) | `spec.rs` `Demand::erosion_stage_stride` | **arbitrary** — a materialization density, not a physics claim: stage bytes are stride-independent (chained ≡ one-shot, convicted), so this chooses only how much settle history is *addressable* | a demand-driven cadence (explorer/beacon asks for the density it needs) or a criterion-driven one, once erosion has an earnable criterion ( `#obs-erosion-residual-is-driver-bound` ) |
| cooling per erosion epoch | 0.05 °C/epoch | `uplift.rs` `TP_COOLING_PER_EPOCH_C` | **arbitrary** — the epoch↔world-time scale as one number; replaces the retired "uplift rate" (whose magnitude was arbitrary *and* whose geography was fake). Measured at 0.05: land rates med ~0.77–0.87 m/epoch (same order as the retired 0.5), rims/sutures to ~5–8, ocean floor −0.24 to −0.34 (signed, zero-mean on the reference grid) | pinned jointly with `epoch ↔ years` when component E lands a calibrated mantle-thermal rate law ( `#form-isostasy-column` FE(8) ) |
| suture crustal thickening | +6 km at full overlap | `lithosphere.rs` `SUTURE_CRUST_M` | **arbitrary** (≈ +1 km stand at full suture) — sized for Abyssal-weak-crust relief, NOT modern Himalayan crust; the strength limit that caps early topography is open physics | a collisional-shortening rate law, when differentiation dynamics land ( `#form-isostasy-column` FE(8) ) |
| craton zonation | 10 % crust span core→rim, **mean-preserving** about the cap's area-mean radial (≈ 2/3) | `lithosphere.rs` `CRATON_ZONATION` | **arbitrary** (≈ 580 m core→rim stand gradient) — real craton zonation exists (Archean cores, younger belts outboard); magnitude declared, not fitted. A non-mean-preserving first cut cost 10–20 % of subaerial cells (measured, cohort comparison) and was corrected same-day. **No drainage-integration claim**: measured at L9 and L13, tilting a radial dome does not change drainage topology — integration awaits collision structure (sutures, arriving at $T_p \lesssim 1450$) or a larger landmass | accretion-age-resolved crust production, when differentiation dynamics land |
| axial tilt, radius, solar constant, year length | 23.44°, 6371 km, 1361 W/m², 365.25 d | `planet.rs` `Planet::EARTH` | **earth-ref** | fine as reference; needs *declaring* in the Phase-1 parameter block (TODO) rather than hardcoding |
| `planet mass` | 5.972×10²⁴ kg | `planet.rs` `Planet::EARTH` | **earth-ref** | ante-mundane (Phase-1) charge; load-bearing for the hydrosphere (water inventory is a fraction of it). Declare in the Phase-1 block with the rest |
| `water mass fraction` (H₂O ÷ planet mass) | 2.3×10⁻⁴ | `hydrosphere.rs` (`WATER_MASS_FRACTION`) | **earth-ref** (contested — deep-mantle water would raise it) | **ante-mundane charge; the honest root of water conservation** — inventory = fraction × planet mass, so rain traces to declared mass, never to nothing. Retires the "planetary water inventory ABSENT" gap below |
| `atmosphere fraction` (of total water) | 9.5×10⁻⁶ (≈ 25 mm global-equivalent) | `hydrosphere.rs` (`ATMOSPHERE_FRACTION`) | **earth-ref** | crude STATIC partition (ocean lumps ice+groundwater); the precip/evap FLOW that makes it dynamic is the climate-nomos rung |
| `atmosphere residence time` | ~9 days (`9.0/365.25` yr) | `climate.rs` (`ATMOSPHERE_RESIDENCE_YR`) | **earth-ref** | the single knob turning the static atmosphere stock into a cycling flux: precip = stock ÷ residence. Yields ~1 m/yr for Earth (order-correct). A real EBM/circulation model replaces the whole identity |
| `precip jitter` | ±50% multiplicative, fated low-frequency fBm (~1000+ km features) | `climate.rs` (`PRECIP_JITTER`) | **arbitrary** (a declared placeholder for *variance*, not a weather model) | Principle (Joseph): uniform rain is a physically impossible state (zero spatial variance), so unmodelled jitter is closer to truth than none — **provided** it is deterministic (fated), mean-preserving (conservation intact), and spatially *correlated* (white noise would be worse than uniform). It buys "there are wet and dry places", NOT "where". Retired by the real structure: **latitudinal ITCZ/Hadley bands** from rotation + insolation (already built), then orography |
| `bounded-fill acceleration` | 9000× | `query.rs` (`FILL_ACCEL`, water-tile) | **arbitrary** (numerical, not physical) | **Replaces the old "rain rate" fudge, and is strictly narrower**: the rain AMOUNT is now principled (climate's ~1 m/yr from the conserved reservoir); this factor only speeds the *fixed-step settle* so basins fill in bounded steps. Retired by the analytic hydrological init (which solves the equilibrium instead of marching to it) |
| vertical shell, voxel anchor | ~20 km, 0.5 m | DESIGN-MATERIAL §8 | **tuned** (decided anchors, rationale recorded) | stands unless a consumer breaks it |
| planetary water inventory | **DECLARED** (2026-07-12) via `hydrosphere` nomos: total = `water mass fraction` × `planet mass` ≈ 1.37×10⁹ km³, conserved, partitioned ocean/atmosphere. "Sea level" as a datum still to be *derived* from the ocean stock (next rung) | `hydrosphere.rs` | **earth-ref** (was: missing) | ocean-stock → sea-level inversion retires `SEA_LEVEL_M`; the reference figures (ocean **1.34×10⁹ km³** [Eakins & Sharman 2010, via Chowdhury 2025 — cited, replacing the from-memory 1.39] ≈ 2.6 km global-equivalent; atmosphere **12,900 km³ ≈ 25 mm** global-equivalent [Gleick 1996, via USGS Water Science School — verified 2026-07-11]). Early Earth: **water generous but contested** (Dong ~1.3 OM central [0.8–2.3]); **basins ~modern-or-shallower, NOT 5–6 km** (majority synthesis; corrected 2026-07-10 — inventory and basin depth are independent levers). The placeholder's ~0.3 km pan stays wrong for every era |
| water fill steps | fixed 200 steps @ dt 0.2 s, rain ×10 (`water-tile` nomos defaults) | `query.rs` | **arbitrary** — and **measured 2026-07-24 not to be a partial settle at all**: per-step residual *grows* monotonically, and 200 steps is **40 s of world time at every level**, because `stable_dt`'s 0.2 s ceiling (the CFL step for ~2 m cells) binds at kilometre cells — 937× below the CFL limit at L9. `#obs-water-fill-never-settles`, `examples/water_settle_probe` | analytic hydrological init seeds the equilibrium instead of marching to it. A convergence-$\varepsilon$ gate is **blocked** here until the step-size question is settled — at this dt it would fire on the small early residual and certify a 40-second transient as converged |
| **`n` from Jarrett** (`1.6`, cap `0.13`) | `n = min(n_base + 1.6·S_bed, 0.13)` — a **static roughness field**, evaluated per pipe from the **BED slope across the face**, never from the water | `water.rs` (**frame kernel**; `WaterParams::{jarrett_slope, jarrett_n_cap}`) | **literature — used as intended (2026-07-29)** | Jarrett 1984 is a regression that **estimates `n` from a measured channel slope**, and this is that use of it. The bed is quasi-static within the fast band, so no feedback can close through it. **MEASURED 2026-07-29 after the demotion:** the ladder's `ρ SHIPPED` equals `ρ n-CONSTANT` at **every** slope (2%–70%) — the roughness term contributes no instability anywhere — while the stabilisation it was credited with **survives and strengthens**: max Fr on the eroded-tile workload falls from a pinned 2.00 to 1.28–1.75 and the supercritical fraction from 0.53% to 0.00–0.07%. ⚠ The **ceiling still binds on ~72% of wet cells** — that is a **terrain** statistic (how much ground is steep enough to saturate the linearisation), not a dynamics one, and it is now **constant in time** (72.6%→72.1%) where the retired form drifted (72.5%→63.9%); the drift *was* the feedback. `examples/roll_wave`, `examples/water_clips`, `WaterSim::clips` |
| **Manning friction discretisation** | **implicit in the UPDATED flux**: `f = A/(1+k·f)`, `k = dt·g·n²/(h^{7/3}·l)`, closed form `2A/(1+√(1+4kA))` | `water.rs` (`pipe_step`) | **derived** (the steady state is dt-free by algebra) | ⛔ **CORRECTED 2026-07-29.** The retired form built the denominator from the **pre-friction** velocity, which made the **steady normal flow a function of Δt**: Fr 1.358 at dt 0.8 s, **1.848 at the shipped 0.2 s**, 2.000 only at dt ≤ 0.05 s — i.e. the shipped pairing ran **~8% slow, one-sided**, and *not* from CFL (the documented `dt ≲ l/√(g·d)` gives 1.53 s there). Implicit, `dt` cancels at steady state leaving `f = l·h^{5/3}·√S/n`. **MEASURED after:** Fr = 2.0000 flat at dt 0.4/0.2/0.1/0.05/0.02/0.01 s. The lone residual at dt 0.8 s is the **outflow clamp** (a cell would ship 24.7 m³ against a 23.04 m³ holding), a different and legitimate limit. ⚠ **The binding accuracy constraint on this kernel is friction, not wave CFL** — `stable_dt` is calibrated to the wrong one. `examples/roll_wave` §2b |
| **$\theta$ flux-smoothing** (de Almeida–Bates) | `0.8` | `water.rs` (**frame kernel**; `WaterParams::{theta, froude_cap}` since 2026-07-24 — variable, so the saturation and $\theta$-sweep probes can run) | ⚠ **NOT "used as intended" — we run outside its published envelope** | ⛔ **ADDED 2026-07-13. de Almeida & Bates 2013 — the applicability study `water.rs` itself cites — bounds the local-inertial model at Fr < 0.5, and states supercritical flow *"cannot be correctly expressed"* by it. MEASURED 2026-07-29 (L19/96² probe footprint): **0.35%** of wet cells run Fr > 1.5, and the breaking cap fires on **0.05–0.21%** of wet cells — cells at a third to a half of mean depth, **not** steeper than average. `max Fr = 2.00` is the cap by algebra, not a measurement; with the cap lifted one step the true demand reaches **2.48**. *(The 2026-07-13 figure of ~5.7% came from the same unmodified instrument — the terrain moved under the 07-24 erosion/router landings; the probe footprint is also submarine, so "eroded land" was never accurate. Do not quote 5.7%.)* ⚠ **DO NOT simply delete $\theta$ as "unphysical."** Measured: $\theta$ **does not remove** the unstable mode — it damps and *relocates* it (still unstable at $\theta=0.5$), because a low-pass filter has no grip on a long wave. **The kernel is RESOLVING a real roll-wave instability it cannot SATURATE** (local-inertial drops the advective term that bounds it). **A scheme that resolves roll waves correctly is a DIFFERENT SCHEME, not a tuned $\theta$.** Principling path: an advection-bearing momentum equation, or an honest declared Fr ceiling. ⚠ Second undeclared deviation, declared 2026-07-29: the sweep applies $\theta$-smoothing **in place** (Gauss–Seidel-flavoured) where de Almeida et al. specify a Jacobi average — measured not mirror-symmetric ($1.9\times10^{-3}$), ~1% extra variance removed; stakes lowered by the roll-wave control (identical $\rho$ for GS vs Jacobi) but the deviation from the cited paper is now on the ledger |
| **momentum closure (scheme class)** | local-inertial: the advective momentum term is **dropped** | `water.rs` (the whole kernel is this choice) | **declared lesser law** (Joseph, 2026-07-29: same rule as every other held-down fidelity — know exactly where we have knobs instead of physics) | The upgrade is an advection-bearing momentum stage (shock-capturing). What the lesser law **forecloses as emergent**: supercritical→jump dissipation — knickpoint faces, plunge-pool scour, outburst-flood carving; today that violence is **parameterized inside the erosion nomos's stream-power $k$**, i.e. an assumption of a seemingly independent nomos — accounted here so it stops being invisible. Per `#norm-physics-not-knobs` FE(2), expect the real term to cost about the same code as the knob-tending it retires, land faster than feared, and be regretted-late — the recorded bias is *reluctance*, not overreach. Revisit trigger, refined 2026-07-29 late: the roll-wave justification for the advective term is retired with the failed identification — the debt is now owed by **the breaking cap's existence** ($\rho$ no-cap 1.023 vs 1.000 shipped at 70% slope: an unphysical stand-in for the missing saturation term), due when any claim depends on supercritical flow being quantitatively right, or when scour wants to be emergent rather than parameterized |
| **breaking cap** | `Fr = 2.0` | `water.rs` (**frame kernel**, hardcoded) | **arbitrary** — and **measured saturated** | See the $\theta$ row. **Promote all four of these literals to `WaterParams` fields: an assumption you cannot vary is an assumption you cannot test** — this currently blocks every water probe. *(Note: the `water.rs` comment's "probe-confirmed: Fr max 226" is the same species of artifact as the "22888" and the saturated `Fr = 2.00` — a clamp or a divide, not a measurement. Do not quote it.)* |
| **water-surface float precision** | `η = bed + depth` in **f32** at a ~4000 m datum ⇒ **1 ULP = 2.44e-4 m** | `water.rs` — `bed: Vec<f32>`, `depth: Vec<f32>` | ⚠ **arbitrary — an undeclared numerical choice that MANUFACTURES A PHYSICAL QUANTITY** | ⛔ **ADDED 2026-07-13.** The scheme is **exactly** well-balanced (below) — but a *settled* lake's f32 $\eta$ carries ~3 ULP of noise and **forges a residual current of 1.6e-4 m/s (≈ 14 m/day) THAT DOES NOT DECAY** (plateaus at 5k–200k steps). The same lake in f64 relaxes to **2.9e-13 m/s** and is still falling. **Floor ratio 5.7e8 vs the ULP ratio 5.37e8 — agree to 6% ⇒ the residual is LINEAR IN ULP.** A **conditioning defect, not a scheme imbalance.** ⚠ **Open harm question (cheap, unrun):** sediment capacity is `C = k·|v|·slope` and `τ = ρgdS` clears `tau_c = 1 Pa` easily in a deep lake ⇒ a nonzero `|v|` on a sloping lake floor is a nonzero **erosion rate** — and each pipe is one-way (`.max(0.0)`), so frozen round-off **could rectify into net bed transport**. Re-run the settle with sediment ON. **Principling path — free, and structurally honest: store the water SURFACE $\eta$ as state instead of depth.** $\eta$ is the smooth, near-constant field; **`depth` is the difference of two large numbers (textbook catastrophic cancellation)** — and **$\eta$ is the field the scheme actually integrates** |

## Structures the code preserves EXACTLY (assets — declare them, and do not break them by accident)

*Added 2026-07-13. These were found by probing, are not written down anywhere, and a well-meaning scheme rewrite could destroy them without noticing.*

| structure | status | why it holds |
|---|---|---|
| **Well-balancedness ("a still lake stays still") — `water.rs`** | ✅ **ACQUITTED, EXACTLY.** Flux = `0.000e0` **bit-exactly** at $t=0^+$ and for 2000 steps after — on flat bed, gentle bumps, **grid-scale roughness**, a **30% slope**, and a **half-dry island**; in **f32 and f64**; at **every refinement**. Not damped — **balanced** | **[D] Structural, not luck.** The pipe momentum drives each face with the water-surface head as a **single term** (`head = η_i − η_j`) and **NEVER SPLITS pressure-gradient from bed-slope — and that split IS the classic failure mode.** At rest $\eta$ is constant ⇒ head = 0 ⇒ accel = 0; the bed enters *only* through `hflow`, a positive factor multiplying zero. It even **survives the wet/dry front**. ⚠ **Do NOT cite de Almeida & Bates for this — they discuss well-balancedness nowhere. It is ours to state.** |
| **Staggering (Arakawa-C) — `water.rs`** | ✅ **ALREADY TRUE, and undeclared.** Depth at centres; the four pipe fluxes **on the faces**; the momentum gradient `(η_i − η_j)/Δx` is a **one-cell reach**. **Two independent probes measured the checkerboard as SEEN** — the most strongly *damped* mode, and felt **10.7× more strongly** than a smooth wave. **`water.rs` has NO null space** | ⚠ **This is a load-bearing asset that a "staggering fix" could destroy by accident.** It also **refutes** the theory doc's earlier claim that the solitons were "the invisible mode" — they are not; **the scheme sees the checkerboard perfectly well.** ⚠ **Corollary: `erosion.rs`'s stream-power/MFD path IS cell-centred and collocated — it is the one that does NOT have this property** |
| **Acyclicity ("water runs downhill and never returns") — routing** | ✅ Enforced by the strictly-downhill clamp — **and never named** | `DECISIONS[corner-pathology-confirmed-but-mfd-does-close]` recorded it as a mystery (*"nothing in the scheme's DESIGN tells you it is needed"*). **It IS in the design: it is the discrete Lyapunov / acyclicity condition.** Declare it and **assert the flow graph is a DAG** |
| rock-mass conservation | **grain-split (2026-07-29 re-true):** pour-grain **enforced and convicted** — the rock-mass ledger conserves crust across the erosion mass-return + isostatic rebound pass (`erosion_return.rs`; `rock_mass_is_conserved_across_the_ledger_pass`; `#form-isostasy-column` FE(9), adopted 2026-07-24). Tile-grain still **not** ledgered — fluvial erosion exports mass through tile outlets uncounted | `erosion_return.rs` (conserved) · `erosion.rs` (open) | **partial** | the tile-grain half: flux-BC / boundary-contract accounting makes outlet exports countable ( `#form-declared-boundary-contract` ; the seam-flux object the halo design names) |

## Known honest fudges (worldview testbench — documented, patch-local)

| constant | value | status | note |
|---|---|---|---|
| rain rate | ~100–1000× real | **tuned** (documented fudge) | fills basins in minutes; analytic init retires it. **RETIRED on the frame path (2026-07-12):** the water-tile nomos now takes its rain from the `climate` nomos (principled ~1 m/yr, the conserved reservoir's throughput), and what remains is only the narrower `bounded-fill acceleration` above. Still live in the worldview testbench, which has its own rain |
| atmosphere store `VIVARIUM_ATMOS` | 2.0 m water-equivalent | **arbitrary** (~80× real ~25 mm) | consistent with the rain fudge; both retire together |
| Lekner–Dorf wet darkening | per paper | **literature** | used as intended (a *render* term; no world-law claim) |
| ~~Jarrett roughness, de Almeida–Bates stabilization~~ | — | ⛔ **MOVED + CORRECTED 2026-07-13** | ⚠ **These were mis-filed here twice over.** (a) **They are NOT testbench-local** — $\theta$ (`0.8`), the Jarrett coefficients (`1.6`, `0.13`) and the Froude cap (`2.0`) were **hardcoded literals inside `crates/vivarium-world/src/water.rs`: the FRAME kernel** (promoted to `WaterParams` fields 2026-07-24 — an assumption you cannot vary is an assumption you cannot test, and now they vary). (b) **"Used as intended" is FALSE for both.** See the two new world-law rows above |

## Operational (not world-law; listed to prevent confusion)

`TILE_NX=64`, CLI default `--level 7`, globe `LEVEL_MIN/MAX 5..9`, relief exaggeration ×20 (HUD-stated), globe default day 91/hour 10 — build/view knobs; they change what is computed or shown, never what is true.

## Process assumptions (meta — how the ledger itself can fail)

- Manual nomos-version constants (`SPINE_VERSION`, `FILL_ALGO_VERSION`) — the §12 known weak link; retired by source-derived versions.
- Earth-reference numbers in this file marked *from memory* are themselves unverified entries until checked (the hypsometry probe's Earth column shares this caveat, and says so on screen).
